# Shared Task Board

Updated: 2026-05-25 (P1-040 PRODUCTION LIVE — site deployed at buildbybimspace.netlify.app · Supabase migrations run · Google OAuth working · LINE Messaging PRD drafted)

Status values: `todo`, `in_progress`, `blocked`, `done`

Sprint marker: Sprint 0-9 preview done (`projects`, `constructionPlanner`, `costCodes`, `suppliers`, `procurement`, `cashflow`, `projectControl`, `approvals`, `evidence`) + Project Control reads Construction Planner baseline rollups, drills down to BOQ task linkage sources with expandable source detail, exports source-detail CSV audit rows, and now tests the source CSV row builder as pure domain logic + BOQ Data pins the route-target source row + Construction Planner sync into Projects/BOQ Data/Project Control baseline + Defect Site Report linked to EvidenceAsset with floor/room/zone/viewpoint metadata, location pin board, Site Report Plan Pin board Phase 1, and Site Report capture sets/same-pin compare + next Site Report slice is comments/comment-to-task + configurable Approval Center/RFQ award evidence gate + project-level RBAC/document authority foundation + BuildDocs authority stamps + Approval Center document authority sync + Admin Project Access UI + BuildDocs/Approval/Projects/export action enforcement + Procurement/Cashflow/Project Control Project Access enforcement + Project Access member directory/topbar selector for multi-level user testing + ERP handoff docs ready + **Sprint 10A mobile foundation: PWA manifest/service-worker/install + Supabase Storage bucket scaffold for evidence files + camera-capture-ready file helper**.

## Current Priority

| ID | Status | Owner | Task | Acceptance |
| --- | --- | --- | --- | --- |
| P1-040 | done | Claude | LINE Messaging Integration PRD (planning only) | Added `docs/LINE_MESSAGING_PRD.md` (430 lines, 13 sections) — vision (replace "tool you must open" with "assistant living in LINE chat"), 3 personas (foreman/PM/owner), 7 prioritized use cases (P0 photo capture + status query, P1 PR commands + receipt OCR, P2 daily digest + voice transcription, P3 group chat), 3 account-linking UX paths (QR scan recommended, manual code fallback, LINE OAuth optional), full architecture diagram (LINE → webhook → Edge Function → Supabase), database schema for 3 new tables (line_user_mappings, line_link_codes, line_message_log) with RLS policies, LINE platform setup checklist (OA + Messaging API channel + webhook config), 6-phase implementation plan (Foundation → Photo → Query → Action → Push → Group), cost analysis showing alpha runs $0 on free tiers (LINE Light $9/mo at ~50 users), 7 open decisions documented for sign-off before Phase 1 (active project detection, multi-workspace, retention, tone, errors, language, rich menu), success metrics, risks + mitigations, out-of-scope list. **Why deferred**: owner explicitly wary of $25/mo Supabase Pro for custom OIDC; LINE Messaging API is the actual killer feature for Thai contractors, not LINE Login. No code shipped yet — pure spec for Sprint 11. |
| P1-039 | done | Claude | Google + LINE + Guest OAuth UI on /account | Extended `src/auth.ts` with `signInWithGoogle()` (wraps supabase.auth.signInWithOAuth({provider:'google'}) with redirectTo=/account) + `signInWithLine()` (provider:'line' cast for Supabase Custom OIDC, defers to Pro plan in production). Redesigned `AccountSignInCard` in `src/PublicSite.tsx` — added 2 OAuth buttons above magic-link form with brand SVG icons (Google 4-color G logo, LINE white-on-green text wordmark); extended busy state union to `''/email/anonymous/google/line`; magic-link form pushed below a horizontal "or magic link" divider; guest mode card kept as second card. Added `.auth-google-btn` (white w/ subtle border), `.auth-line-btn` (#06C755 LINE brand green), `.auth-divider` (horizontal lines flanking small uppercase label) to `src/styles.css`. Wrote `docs/AUTH_OAUTH_SETUP.md` (~150 lines) with step-by-step Google Cloud Console setup (project + OAuth consent screen + Web client + Authorized origins + redirect URIs), Supabase Provider enable, LINE Developers Console alternative path (requires Pro plan), troubleshooting matrix (redirect_uri_mismatch, access blocked, redirect mismatch, missing env vars). **Production validated**: owner ran through Supabase URL Configuration (Site URL + Redirect URLs allowlist for /account + wildcards + localhost) + Google Cloud Console (OAuth consent screen as External + Web client with both buildbybimspace.netlify.app and mnanqmmgniaqpvkzupmn.supabase.co origins + supabase callback redirect URI) + pasted Client ID/Secret into Supabase Google provider → Google login working end-to-end on production. 580/580 tests pass at time of commit, vite build clean. **Open follow-up**: rotate Google Client Secret because it leaked in chat log during setup (low risk but should be done). |
| P1-038 | done | Claude | Deploy infrastructure + production launch | **Deploy infra**: Updated `.gitignore` to exclude local-only configs (`.claude/`, `.agents/`, `.mcp.json`, `tmp/`, `artifacts/`) and OS cruft (`.DS_Store`, `Thumbs.db`, `.vscode/`, `.idea/`). Created `supabase/migrations/_all-in-one.sql` (1672 lines — concatenation of 0001→0013 with section banners so user can paste once instead of opening 13 files). Created `supabase/migrations/_reset-public-schema.sql` (43 lines, idempotent) for re-deploy recovery — drops 4 storage.objects RLS policies + nukes public schema cascade + restores Supabase default grants (auth/storage/extensions preserved); also fixed initial version that had DELETE FROM storage.* which Supabase trigger blocks (42501) — relies on migration 0013's ON CONFLICT DO UPDATE for bucket idempotency instead. Wrote `docs/DEPLOY.md` (~180 lines) — 6-step click-by-click guide from "first commit" to "live with PWA install + camera flow verified": GitHub repo creation, Supabase migration paste, Netlify signup via GitHub OAuth, env vars (VITE_SUPABASE_URL/VITE_SUPABASE_ANON_KEY/VITE_SUPABASE_SYNC_ENABLED), 4-URL verification matrix, PWA install + camera flow on mobile, optional custom domain DNS guide, common-errors table. Rewrote `README.md` for v0.4.28 state (replaced MVP-era doc that only mentioned BuildDocs with 12-app workspace overview + ERP loop + mobile features + tech stack + deploy pointer + dev local instructions). Added `public/robots.txt` (allow 7 public marketing pages, disallow all workspace/admin routes since they require auth) + `public/sitemap.xml` (7 URLs with priority + changefreq for search engine indexing). **Production launch** (owner ran the steps): GitHub repo `buttumpan1919-pixel/buildbybim` created (private) + 7 commits pushed (auth via gh CLI with both `buttumpan1919-pixel` and `narongsak-art` accounts added — `buttumpan1919-pixel` active; git push uses inline credential helper `git -c credential.helper='!gh auth git-credential'` because Windows Credential Manager has stale narongsak-art entry that overrides gh active account). Supabase project `mnanqmmgniaqpvkzupmn` schema migrations run via _all-in-one.sql (after _reset-public-schema.sql cleared stale partial state from 42710 policy-exists error). Netlify site `buildbybimspace.netlify.app` connected to GitHub repo, auto-deploys on push to main, env vars set in Netlify dashboard. **Verified live**: curl 200 OK for /, /manifest.webmanifest, /sw.js, /robots.txt; landing page title renders correctly; manifest JSON parses; asset bundle hash changes per deploy (confirmed pipeline live). **Memory saved**: `memory/project_deployment_status.md` + `memory/project_line_strategy.md` for context preservation across sessions. **Total session output**: 7 commits (`3396df6`, `9bf500a`, `e14a6a7`, `1a946f0`, `7a0d8e1`, `9b20c1f`, + this PRD update), 184 files in initial commit, ~3500 lines of docs across DEPLOY.md + LINE_MESSAGING_PRD.md + AUTH_OAUTH_SETUP.md + README rewrite. |
| P0-001 | done | Codex | Create central PRD/docs for multi-agent development | `docs/PRD.md`, `docs/ARCHITECTURE.md`, `docs/AGENT_WORKFLOW.md`, `docs/TASKS.md`, `docs/DECISIONS.md` exist |
| P0-005 | done | Codex | Rewrite master PRD as app-by-app source of truth | `docs/PRD.md` documents no-Firebase local-first direction, app-by-app workflow, and BOQ Task-to-BOQ linkage spec |
| P0-002 | done | Codex | Stabilize source-of-truth docs against current app behavior | App Map ใน `docs/PRD.md` ตรงกับ `src/apps.ts` (12 apps + admin); routes ตรงกับ `src/main.tsx` + `src/PublicSite.tsx`; taxonomy fields ใน manifest ตรงกับ APP_TAXONOMY.md Section 2; TASKS.md Done Log ครอบคลุมงาน session ทั้งหมด |
| P0-003 | done | Codex | Add basic test strategy for storage and CSV parsing | Vitest + jsdom ติดตั้งแล้ว; 77 tests ผ่าน ครอบคลุม storageAdapter, membership access evaluation, cashflow, SupabaseAdapter, `parseSheetCsv`/Google Sheet CSV URL, `calculateTotal`, และ workspace import/save normalizers |
| P0-004 | done | Codex | Reduce conflict risk in `src/App.tsx` | `docs/APP_TSX_MODULE_SPLIT_PLAN.md` identifies low-risk split order, target folder shape, guardrails, and verification checklist before moving code |
| P0-017 | done | Codex | Start `App.tsx` module split phase 1 | Moved `PageHeader`, `SummaryTile`, `CashflowPanel`, `AgentChatPanel`, `DesignStudioPanel`, and `LibraryPanel` into `src/workspace/*`; `App.tsx` reduced from ~10,895 to ~9,660 lines; test/build/browser smoke passed |
| P0-018 | done | Codex | Start `App.tsx` module split phase 2 | Moved social feed types/helpers/storage into `src/workspace/apps/social-feed/socialFeedService.ts`; added focused service tests; `App.tsx` keeps `SocialFeedPanel` UI only; 83 tests, build, and browser smoke `/feed` passed |
| P0-019 | done | Codex | Continue `App.tsx` module split phase 2 with employees | Moved employee project options, normalization, site-team derivation, legacy storage cleanup, payroll helpers, and app stats into `src/workspace/apps/employees/employeeService.ts`; 88 tests, build, and browser smoke `/employees` passed |
| P0-020 | done | Codex | Continue `App.tsx` module split phase 2 with defects | Moved defect labels, legacy storage cleanup, file-size/date formatters, and image upload helper into `src/workspace/apps/defects/defectService.ts`; 92 tests, build, and browser smoke `/defect` passed |
| P0-021 | done | Codex | Continue `App.tsx` module split phase 2 with BOQ Data | Moved BOQ catalog storage, row normalization, search text, merge rules, filter/status constants, and CSV parsing into `src/workspace/apps/boq-data/boqDataService.ts`; added focused service tests; 193 tests, build, and Playwright smoke `/boq-data` database + task-linkage passed |
| P0-022 | done | Codex | Start `App.tsx` module split phase 3 with workspace shell helpers | Moved route parsing/building, subnav definitions, version selection storage, language persistence, shell copy, app groups, and app icons into `src/workspace/shell/*`; added focused routing/language tests; current suite 381 tests and build passed |
| P0-023 | done | Codex | Add public Roadmap page and update workflow rule | `/roadmap` is a public route backed by `src/roadmap.ts` and rendered by `src/public/RoadmapPage.tsx`; public nav links to Roadmap; docs require updating Roadmap after completed development work; test/build/route smoke recorded |
| P1-001 | done | Codex | Builk parity Sprint 0 — Project entity + List/Detail app | `src/projects.ts` data layer (Project + ProjectListState + ProjectComputed + storage via adapter + suggestNextProjectCode + computeProject + applyAutoStatus + summarizeProjectList + detectCustomerType + createSeedProjects + ensureSeedProjects); `src/projects.test.ts` 40 tests; new `projects` app in `src/apps.ts` (route `/projects`, group Project Work, taxonomy: construction/freemium/write/workspace/optional AI); `src/workspace/apps/projects/ProjectsPanel.tsx` (list with carousel + 5-state filter + search + create form + sortable table + auto-detect customerType; detail with 7-tab nav + overview alerts + cost-breakdown placeholder + AI insights card + edit/delete); `supabase/migrations/0003_projects.sql` (16-field projects table + 4 indexes + RLS via `is_workspace_member` + updated_at trigger). 138 tests pass · build clean · `/projects` returns 200 |
| P1-002 | done | Codex | Builk parity Sprint 1 — Cost Codes (CBS) catalog + import/export | `src/costCodes.ts` data layer (CostCode + CostCodeState + storage via adapter + validateCostCode (unique-per-workspace + parent-exists + custom-unit + no-self-parent) + upsertCostCode + deactivateCostCode (soft) + removeCostCode + searchCostCodes + filterCostCodesByCategory + groupCostCodesByParent + summarizeCostCodes + parseCsv + mapUnitLabelToCostCodeUnit (Thai/EN) + parseCostCodeCsv + applyCsvImport (skip_duplicates vs update_existing) + exportCostCodesToCsv); `src/costCodes.seed.ts` 100 Thai cost codes across 7 categories (site/structure/architecture/mep/finishing/external/indirect); `src/costCodes.test.ts` 55 tests; new `costCodes` app in `src/apps.ts` (route `/cost-codes`, new sidebar group `procurement` "Cost Control", taxonomy: construction/freemium/write/workspace/optional AI); `src/workspace/apps/cost-codes/CostCodesPanel.tsx` (Catalog tab: tree view by category + collapsible + search + add/edit form with validation + soft-delete; Import tab: paste/file CSV + parse preview table + skip vs update toggle + apply; Export tab: download CSV; Usage stub TH/EN); `supabase/migrations/0004_cost_codes.sql` (cost_codes table + 4 indexes + RLS read system-seed shared + write workspace-only + updated_at trigger + 7 root seed rows). 193 tests pass · build clean · `/cost-codes` returns 200 |
| P1-005 | done | Codex | Builk parity Sprint 4 — RFQ comparison + award + auto price history | Extended `src/procurement.ts` with RFQ state machine `transitionRFQStatus` (6 states × 5 actions: send / record_response / complete / award / cancel) + `recomputeRFQStatus` auto-status from response coverage (sent → partial_response when first response → responses_complete when all invited responded) + `recordResponse` validates invited + auto recomputes + `removeResponse` reverts complete→partial + `buildComparisonMatrix` item×supplier table with per-row best-price flag + per-supplier total + best-total computed from fully-quoted suppliers (falls back to partial when none complete) + AwardReason enum (lowest_price/best_payment_terms/fastest_delivery/preferred_vendor/other) + `awardRFQ` side-effect-free function returning {rfq awarded, pr advanced to awarded, priceHistoryAppendages[]} with explicit linkedRfqId stamping + cross-validation (supplier invited / response present / PR matches / reason given) + filterRFQs/summarizeRFQs/removeRFQ; `src/procurement.rfq.test.ts` 44 tests (storage + numbering / draftRFQFromPR throws on non-approved + preferred-supplier seeding / state machine all 5 actions × happy + illegal + context-required / upsert/remove / recordResponse rejects non-invited + auto-transitions sent→partial→complete + dedup by supplier + removeResponse reverts / recomputeRFQStatus leaves terminal alone / buildComparisonMatrix highlights cheapest + computes amount from qty + marks unavailable cells + bestTotal from fully-quoted + falls back to partial + zero rows / awardRFQ happy path stamps PR + builds priceHistoryAppendages with cost code + 5 error cases / filter+summarize); ProcurementPanel rewired — wired PR "Send RFQ" action to `draftRFQFromPR` + `nextRfqNumber` + opens new RFQ detail via `setQueryParam`; replaced RFQ stub with **RFQ list tab** (7-status filter chips + 6-col table with PR No + project + response count) + **RFQ detail** (3-section layout: header with status badge + send/cancel/delete actions; Invite suppliers as multi-select checkboxes from suppliers directory; Record responses inline per invited supplier with per-item unit-price input + no-bid toggle + payment terms/delivery/validUntil/notes + edit/remove; Compare matrix item×supplier with cheapest-cell highlight + per-supplier total row with ★ BEST TOTAL + Award button per supplier with reason picker via `window.prompt`); Award flow updates RFQ + PR + appends to SupplierPriceHistory via `addPriceHistoryEntry`; URL `?rfqId=` driven detail. TH/EN copy 24 new strings. `supabase/migrations/0007_rfqs.sql` (3 tables: rfqs + rfq_responses + rfq_item_quotes + 9 indexes + 12 RLS policies + updated_at trigger; nested via rfqs.workspace_id). 351 tests pass (44 new) · build clean (workspace 507 KB gzip 127 KB) · `curl /procurement?tab={pr-list,rfq-list,archive}` returns 200 |
| P1-007 | done | Codex | Builk parity Sprint 6 — Project Control Dashboard + 5 standard reports | Created `src/csvExport.ts` shared utility (RFC 4180 quote/escape + UTF-8 BOM for Thai-safe Excel + `rowsToCsv` + `downloadCsv` + `dateStampForFilename`); `src/projectControl.ts` pure aggregation engine (no persisted entities except settings) — `computeProjectSnapshot(ctx)` groups confirmed cashflow expense by cost code → actual, approved/rfq_sent/awarded/ordered/received PRs → committed; budget = committed when PR exists, else proportional from project.plannedCost; produces `costCodeRollups[]` sorted by spent% desc with `isOverBudget` flag + `totalBudget/Committed/Actual/Revenue/PaidRevenue/netCashflow/marginPct/daysRemaining`; cost code key normalized to handle both `CostCode.id` (from cashflow) and `CostCode.code` (from PR line items); `generateAlerts` 7 types (over_budget critical / near_budget warn at 85% / overdue critical when normal+delayed / pending_invoice warn / stale_pr warn at 30d / low_margin warn/critical < 10% (critical when negative) / no_recent_activity info > 14d) sorted critical→warn→info; 5 report generators (`generateProjectPL`, `generateCashflowForecast` 90-day window bucketed by month, `generateCostVariance` row-per-(project,cost-code), `generateSupplierSpend` top-10 last 12 months, `generatePRAging` excludes terminal status) + `generateReport` dispatcher; `summarizeWorkspaceControl` for Hub tile use; ProjectControlSettings storage + normalize with default thresholds (85/10/30/14); `src/projectControl.test.ts` 30 tests (settings round-trip, snapshot 5 cases — actual confirmed only / committed PR filter / project scoping / null margin internal / order by spent desc / isOverBudget; alerts 7 type cases + threshold respect + severity sort; 5 reports happy + filter + edge cases; workspace summary); `src/csvExport.test.ts` 14 tests (escape per RFC 4180 + BOM + CRLF + Thai + boolean serialize). Added `projectControl` to `src/apps.ts` + shell wiring (LineChart icon, procurement sidebar group as fourth member, subnav dashboard/reports/settings, TH/EN copy). Built `src/workspace/apps/project-control/ProjectControlPanel.tsx` — 4 summary tiles workspace-level (Projects / Over budget / Low margin / Stale PR); **Dashboard tab**: project picker dropdown + 7 KPI tiles (Budget/Committed/Actual/Remaining/PaidRevenue/Net/Margin) + alerts banner sorted by severity with action deep-links + cost breakdown bars per cost code (color: green <85%, yellow 85-100%, red >100% with row tint) + AI insights dark card placeholder + refresh button; **Reports tab**: scope dropdown (all/per-project) + 5 report cards as clickable buttons → render result table with CSV download (UTF-8 BOM filename `{type}-YYYY-MM-DD.csv`) + window.print(); **Settings tab**: 4 threshold number inputs + default report dropdown + save/reset defaults + saved confirmation. TH/EN copy 56 strings. Wired into `WorkspaceAppPanel` after Procurement; `supabase/migrations/0009_project_control_settings.sql` (project_control_settings single-row-per-workspace table + JSONB alert_thresholds with default values + CHECK constraint on default_report_type + workspace-only RLS + updated_at trigger). Print CSS rules in `styles.css` @media print — hide nav/topbar/summary chrome via `.no-print`/`.cf-topbar`/`.module-hero`/`.summary-grid`, strip borders on `.project-control-print` for clean PDF output. 429 tests pass (44 new: 30 projectControl + 14 csvExport) · build clean (workspace 546 KB gzip 135 KB) · `curl /project-control?tab={dashboard,reports,settings}` returns 200 |
| P1-008 | done | Codex | Builk parity Sprint 7 — Approval + Audit Core | Added `src/approvals.ts` generic approval state machine/storage/sync helpers + `src/approvals.test.ts`; added `approvals` app route `/approvals` with Inbox/History/Rules and approve/reject source sync for PR + Cashflow; decisions write membership audit via `appendAuditEntry`; added `supabase/migrations/0010_approval_requests.sql`; docs updated in `docs/APPROVAL_AUDIT_PRD.md`, `docs/ERP_ADAPTATION_PLAN.md`, and `docs/POJJAMAN_GAP_ROADMAP.md`. |
| P1-009 | done | Codex | ERP implementation handoff docs after ERD | Added `docs/DATA_DICTIONARY.md`, `docs/WORKFLOW_SEQUENCE.md`, and `docs/SUPABASE_SYNC_CONTRACT.md`; wired references into `docs/PRD.md` and `docs/IMPLEMENTED_ERD.md`; next implementation direction clarified as Evidence Asset Layer then Phase D relational mappers. |
| P1-010 | done | Codex | Builk parity Sprint 8 — Evidence Asset Layer | Added local-first evidence service, `/evidence` workspace app, Supabase migration `0011_evidence_assets.sql`, PRD/ERD/Data Dictionary/Sync Contract updates, and tests for evidence storage, file intake, links, filters, status, summary, and route wiring. |
| P1-011 | done | Codex | Evidence approval soft guard | Added evidence requirement helpers and wired Approval Center to show direct verified evidence status per request, open Evidence from approval rows, and confirm before approving a PR/Cashflow item without direct verified evidence. |
| P1-012 | done | Codex | Configurable approval evidence gate | Added `evidence.approval-policy.v1` with `off/warn/block`, amount threshold, and target-type selection; Approval Center Rules tab can persist the gate and Inbox enforces block/warn decisions before approval. |
| P1-013 | done | Codex | RFQ award evidence gate | Procurement RFQ award now checks the saved evidence policy and blocks/warns high-value awards without direct verified evidence linked to the RFQ; RFQ detail shows current Evidence Gate state and can open Evidence. |
| P1-014 | done | Codex | Project RBAC + document authority foundation | Added `src/projectAccess.ts` for project-level grants, supplier scoping, permission decisions, and BuildDocs authority stamps (`prepared/submitted/checked/approved/issued`); added `supabase/migrations/0012_project_access_document_authority.sql`; updated ERD/Data Dictionary/Sync Contract/PRD handoff docs. |
| P1-015 | done | Codex | BuildDocs document authority UI wiring | Wired document authority state into BuildDocs panel, legal status actions, audit writes, preview/PDF stamp grid, and print-safe CSS. |
| P1-016 | done | Codex | Approval Center document authority integration | Approval Center now syncs BuildDocs PO/Invoice `sent` documents into inbox; approve/reject updates document status and writes approved/void authority stamps linked to the approval request. |
| P1-023 | done | Codex | Project Access Projects/export enforcement | Projects now scopes list/detail/analytics to readable project grants and gates create/edit/delete by `project.write`/`project.admin`; BuildDocs print/PDF, share/send, and workspace backup export now require `document.export`/`settings.manage`. Verification: `npx tsc --noEmit` pass, `npm test -- --run` 531/531 pass, build pass with existing Vite chunk warning. |
| P1-024 | done | Codex | Project Control BOQ source drill-down | Project Control cost breakdown rows now carry Construction Planner source links and expose BOQ source buttons; `/boq-data?tab=task-linkage` reads `projectId`, `taskId`, `boqItemId`, and `costCode` query context to select/highlight the originating task/linkage. Also fixed a ProjectsPanel `WorkspaceLanguage` type import blocker. Verification: focused Project Control tests pass 38/38, `npx tsc --noEmit` pass, full suite 536/536, build pass with existing Vite chunk warning, Browser smoke Project Control → BOQ Data drill-down + `/roadmap` pass. |
| P1-025 | done | Codex | Site Report 360 coordination PRD | Added `docs/SITE_REPORT_360_PRD.md` as the clean-room product spec for Plan Pin board, capture sets, same-pin compare, comments, comment-to-task, File Center, report templates, data/storage, permissions, and phased delivery. Linked it from Master PRD, BUILK360 analysis, TASKS, and Roadmap. Verification: `npm test -- --run` pass 536/536, `npx tsc --noEmit` pass, `npm run build` pass with existing Vite chunk warning, route smoke `/roadmap` + `/defect?tab=site-report&version=0.1` pass. |
| P1-026 | done | Codex | Project Access financial/control enforcement | Added shared local Project Access helpers and wired grant-based scoping into Procurement, Cashflow, and Project Control. Procurement now scopes PR/RFQ lists/details and guards PR save/delete/actions plus RFQ send/response/delete/award with `procurement.write`/`procurement.approve`; Cashflow scopes visible entries/projects and guards draft/confirm/delete with `cashflow.write`/`cashflow.approve`; Project Control scopes dashboard/report context by `report.read`. Verification: `npx tsc --noEmit` pass, `npm test -- --run` 536/536 pass, `npm run build` pass with existing Vite chunk warning. |
| P1-031 | done | Codex | Project Control source detail + BOQ route focus hardening | Project Control cost breakdown source blocks now expand to all Construction Planner BOQ source rows instead of only the first three actions; BOQ Data task-linkage pins the route-target BOQ row even when search/filter would hide it; corrected the bad `ยท` separator in task-linkage cost-code labels; fixed an Approval Center ternary typo that blocked typecheck. Verification: `npx tsc --noEmit` pass, `npm test` 587/587 pass, `npm run build` pass with existing Vite chunk warning. |
| P1-032 | done | Codex | Project Access actor switcher for multi-level user testing | Added `project-access.local-actor.v1`, `PROJECT_ACCESS_TEST_ACTORS`, local actor load/save helpers, and a workspace topbar selector for Site Manager / Project Owner / Procurement / Accounting / Reviewer / Viewer / Vendor / Workspace Admin. Projects, Procurement, Cashflow, Project Control, Approval Center, and BuildDocs now read the selected local actor through shared helpers, so Builk-style user-level access can be tested without changing grants every time. Verification: `npx tsc --noEmit` pass, focused tests 27/27 pass, full suite 587/587 pass, `npm run build` pass with existing Vite chunk warning. |
| P1-033 | done | Codex | Site Report Plan Pin board Phase 1 | Added local-first `siteReportService` helpers for `site-report.plans.v1` and `site-report.pins.v1`, wired `/defect?tab=site-report&version=0.1` with plan creation, numbered x/y plan pins, Location pin binding, and plan-pin selection that scopes report events/evidence. Updated PRD, Site Report PRD, Data Dictionary, Sync Contract, Architecture, BUILK360 analysis, Roadmap, and TASKS. Verification: `npx tsc --noEmit` pass, `npm test -- --run` 587/587 pass, `npm run build` pass with existing Vite chunk warning, HTTP smoke `/defect?tab=site-report&version=0.1` + `/roadmap` 200, Chrome CDP smoke added plan/evidence/pin and confirmed 1 plan / 1 pin, active Location pin, filtered evidence, `/roadmap` v0.4.32, and no console/network errors. |
| P1-034 | done | Codex | Project Control source CSV export | Added a separate Project Control dashboard export for source-detail CSV rows with generated_at, project, cost code, task, BOQ item, amount, and source fields. The button is disabled when the current snapshot has no Construction Planner source rows and keeps the original rollup CSV export unchanged. Verification: `npx tsc --noEmit` pass, `npm test -- --run` 594/594 pass, `npm run build` pass with existing Vite chunk warning, HTTP smoke `/project-control?tab=dashboard&version=0.1` + `/roadmap` 200, Browser smoke confirmed the source CSV button is visible/enabled and `/roadmap` shows v0.4.36. |
| P1-035 | done | Codex | Project Access member directory | Added `project-access.members.v1`, member normalization/load/save/upsert helpers, active-member actor listing, and a same-tab update event so Admin Project Access and the workspace topbar share one local user directory. Admin Project Access now shows a member directory board, can pick/use existing members, and saves custom members when creating grants; topbar label changed to `Member`/`ผู้ใช้งาน` and reads active members instead of hardcoded test actors. Verification: `npx tsc --noEmit` pass, focused `projectAccess` tests 25/25 pass, full suite 594/594 pass, `npm run build` pass with existing Vite chunk warning, Browser smoke `/admin?tab=project-access&version=0.1`, `/procurement?tab=pr-list&version=0.1`, and `/roadmap` pass. |
| P1-036 | done | Codex | Project Control source CSV helper tests | Moved Project Control source CSV header/row construction into `src/projectControl.ts` as `PROJECT_CONTROL_SOURCE_CSV_HEADERS` + `buildProjectControlSourceCsvRows`, so the dashboard UI only triggers download and the export contract is covered by unit tests. Added 3 tests for stable header order, source-link flattening, and empty-source omission. Verification: focused `projectControl` tests 41/41 pass, `npx tsc --noEmit` pass, `npm test -- --run` 597/597 pass, `npm run build` pass with existing Vite chunk warning, HTTP smoke `/project-control?tab=dashboard&version=0.1` + `/roadmap` 200, Browser smoke confirmed source CSV button still visible/enabled and `/roadmap` shows v0.4.38. |
| P1-037 | done | Codex | Site Report capture sets + same-pin compare | Added local-first `site-report.capture-sets.v1`, capture set normalization/load/save/upsert helpers, reserved EvidenceAsset tags (`site-plan:*`, `site-pin:*`, `site-capture:*`), plan-pin/capture filters, capture set board, evidence plan-pin/capture selectors, and same-pin reference/current compare in `/defect?tab=site-report&version=0.1`. Updated PRD, Site Report PRD, Data Dictionary, Sync Contract, Architecture, BUILK360 analysis, Roadmap, and TASKS. Verification: focused Site Report/Defect tests 16/16 pass, `npx tsc --noEmit` pass, `npm test -- --run` 597/597 pass, `npm run build` pass with existing Vite chunk warning, HTTP smoke `/defect?tab=site-report&version=0.1` + `/roadmap` 200, Browser smoke created 1 plan / 1 pin / 2 capture sets / 2 tagged evidence and confirmed `reference 1 / current 1` plus `/roadmap` v0.4.39. |
| P1-030 | done | Claude | First-run onboarding checklist on Hub | Added `src/workspace/shared/FirstRunChecklist.tsx` — pure presentation component that renders a 4-step guided checklist on the Hub home when the workspace is empty (0 projects AND 0 cashflow entries). Steps: ① Create first project → `projects` app · ② Pick standard cost codes → `costCodes` app · ③ Record first cashflow entry → `cashflow` app · ④ View dashboard → `projectControl?tab=dashboard`. Progress derived from live state: step 1+2 unlock once `projects.length > 0`, step 3 unlocks once `cashflow.entries.length > 0`, step 4 ✓ once both. Checklist hides entirely once both gates pass so returning users see a clean Hub. TH/EN copy inline. Wired into `App.tsx` Hub branch above `summary-grid` with 1 import + 1 JSX block — reads existing `projects` + `hubCashflowState.entries` (no new data fetches). Added `.first-run-checklist` styles in `src/styles.css` with indigo/violet gradient header (Sparkles icon), white step cards that turn green on `is-done`, mobile breakpoint at 720px stacks the action button below text. **Why**: Earlier strategic analysis flagged "10-min first-value flow" as the missing piece before alpha tester recruitment — Hub was a generic 11-app launcher with no guided path, so new users had to guess where to start. Verification: `npx tsc --noEmit` pass, `npm test -- --run` 580/580 pass (no new tests — component is pure presentation, no domain logic), `npm run build` pass with existing Vite chunk warning. Roadmap bumped to v0.4.28 with label "first-run checklist on Hub (4-step guided onboarding for alpha testers)". **Next**: copy this content into Gmail (user must initiate) + recruit 1 alpha tester + observe whether the checklist actually compresses time-to-first-value. |
| P1-029 | done | Codex | Sprint 10B relational sync engine + 2-way Sync tables button | **Sync engine**: Created `src/supabase.sync.ts` (pure-ish — accepts a `SupabaseLikeClient` interface so tests can stub without supabase-js). Three operations: `pushToSupabase(client, workspaceId, loaders?)` upserts every local row to `projects` / `cashflow_entries` / `purchase_requests` / `pr_line_items` with `onConflict: "id"` so re-runs are idempotent; workspace filter applied before push; PR head + line items pushed together (head first, then items keyed by pr_id). `pullFromSupabase` selects all rows from the 4 tables, filters by `workspace_id` client-side (RLS does this too on cloud), then merges by id with last-write-wins via `resolveByUpdatedAt`. `twoWaySync` runs pull first then push so cloud catches up with local-won conflicts — short-circuits if pull errors so we don't push possibly-stale state. `SyncReport` returns per-entity counts (pushed / pulled / unchanged / conflictsLocalWon / conflictsRemoteWon) + ok/error string for UI. `SyncLoaders` injectable so production uses live storage adapter while tests use in-memory state. **Tests** (`src/supabase.sync.test.ts` 14 tests): stub client tracks every upsert/select call · push 1 project with correct `onConflict: id` · push cashflow scoped to workspace · push PR head + line items together · push surface error on single-table failure · pull remote-only project · keep local on local-newer · overwrite local on remote-newer · filter remote rows by workspace_id · merge PR head + items with line-item id filtering · pull surface error on select fail · cashflow round-trip preserves all Sprint 5 fields · twoWaySync calls pull-then-push in order · twoWaySync short-circuits on pull error without push. **UI**: Added `handleRelationalSync` in `src/AdminPanel.tsx` — wraps the real supabase-js client into `SupabaseLikeClient` (compatible at runtime), runs `twoWaySync`, sets `syncMessage` with pushed/pulled/conflict counts, reload()s on success. New "Sync tables (2-way)" primary-button placed between Pull and Disconnect in the Cloud Sync card. Extended `syncBusy` union with `"relational"` for the in-flight state. TH/EN copy added. **Verification**: `npx tsc --noEmit` pass, `npm test -- --run` 580/580 pass (14 new), `npm run build` pass with existing Vite chunk warning, smoke `/admin?tab=overview` + `/evidence?tab=intake` return 200. **Trust loop closed**: ผู้ใช้สมัคร → ถ่ายรูป upload (Sprint 10A) → กด "Sync tables" → เปลี่ยน device → กด "Sync tables" → data ตรงกันทุกฝั่ง. **Next**: alpha tester recruitment or Sprint 10C realtime + payment. |
| P1-028 | done | Codex | Sprint 10A camera capture + cloud upload + relational mappers | **Evidence intake camera UX**: Added `accept="image/*" capture="environment"` file input variant to `EvidencePanel` so contractors on mobile open the back camera with one tap (separate from the general file picker that still accepts PDF/Excel/etc.). Added `evidence-file-drop--camera` CSS variant (purple tint) for visual differentiation from the general file picker (green tint). Added 4 new TH/EN copy keys (`takePhoto`/`takePhotoHint`/`cloudReady`/`localOnly`) so the form shows the storage state explicitly. **Cloud upload path**: Added `EvidenceAsset.storageBucket` + `storagePath` fields (matching migration 0013) with normalize defaults to empty string for back-compat. Added `createEvidenceAssetFromFileWithCloud` factory in `src/evidence.ts` — uploads via the caller-injected `uploadFn` (so the function stays pure of Supabase imports) and on failure falls back to the local dataUrl path with a `pending-cloud-upload` tag so the relational mapper (P1-029 next) can retry. Wired EvidencePanel save logic to prefer cloud upload via `isStorageAvailable()`. Added a "☁ Open" button in the evidence list that fetches a short-lived signed URL via `getEvidenceSignedUrl` when `storagePath` is set (dataUrl link still shown when present). Updated `defectService.test.ts` literal EvidenceAsset constructions with the 2 new fields. **Relational mappers**: Added `src/relationalMapper.ts` with pure bidirectional converters for the 3 core entities — `projectToRow` / `rowToProject` (handles client_id ↔ "" + customer_type null + status validation), `cashflowEntryToRow` / `rowToCashflowEntry` (round-trips all Sprint 5 fields including sourceType/projectId nullability), `purchaseRequestToRow` + `prLineItemToRow` (head + items split matching 0006 schema, with requested_by/approved_by/needed_by_date null-mapping), `rowToPurchaseRequest` (filters line items by pr_id so caller can pass workspace-wide item rows safely + auto-recomputes amount via normalizePRLineItem); `resolveByUpdatedAt` last-write-wins conflict helper returning 6 reasons (local_only/remote_only/both_null/local_newer/remote_newer/tied_remote_wins with remote-trust on tie). 17 tests in `src/relationalMapper.test.ts` covering full round-trips, nullable field handling, status fallback, sourceType preservation, line-item filtering, normalizer re-derivation, and all 6 conflict outcomes. **No supabase-js imports in the mapper** — keeps the module pure and unit-testable; UI/sync glue (Sprint 10B) injects the Supabase client at the boundary. Verification: `npx tsc --noEmit` pass, `npm test -- --run` 566/566 pass (17 new mapper + camera UX confirmed via build), `npm run build` pass with existing Vite chunk warning, smoke `/evidence?tab=intake` + `/manifest.webmanifest` + `/sw.js` return 200. |
| P1-027 | done | Codex | Sprint 10A mobile-readiness foundation — PWA + Supabase Storage bucket scaffold | **PWA**: added `public/manifest.webmanifest` (standalone display + 3 home-screen shortcuts: Hub, Cashflow, Defect Site Report) + `public/icon-app.svg` + `public/icon-apple-touch.svg` + `public/sw.js` service worker (cache-first app shell, network-first elsewhere with offline fallback, bypass Supabase API hosts, version-bumped cache invalidation); `src/pwa.ts` registers SW in production only with `beforeinstallprompt` capture for "Install" UI; `index.html` wired manifest link + apple-touch-icon + `apple-mobile-web-app-*` meta. **Storage**: `src/storage.client.ts` wrapper for Supabase Storage (uploadEvidenceFile + getEvidenceSignedUrl + removeEvidenceFile + fileToDataUrl local fallback + readImageDimensions + formatFileSize + buildEvidencePath sanitizer); `supabase/migrations/0013_evidence_storage_bucket.sql` creates `evidence-files` bucket (private, 25 MiB limit, image/PDF/video mime allowlist) + 4 workspace-scoped RLS policies on `storage.objects` keyed by first folder segment + adds `storage_bucket`+`storage_path` columns to `evidence_assets` for the Sprint 10B relational mapper. **Auth**: confirmed `src/supabaseClient.ts` already uses token-based persistence (`persistSession: true` + `autoRefreshToken: true` via localStorage `buildbybim.auth.v1` key) — works for both Web + future React Native via shared storage adapter swap. `src/storage.client.test.ts` 13 tests (path build/sanitize/truncate/empty-guard + isStorageAvailable + formatFileSize + fileToDataUrl). Updated `docs/SUPABASE_SETUP.md` with migration index (12→13) + new Section 6B (bucket setup + path convention + signed URL flow + local fallback) + Section 6C (PWA install verification steps). Verification: `npx tsc --noEmit` pass, `npm test -- --run` 549/549 pass (13 new), `npm run build` pass with existing Vite chunk warning, preview server confirms manifest served as `application/manifest+json` + sw served + icons served. **Next**: Sprint 10A continuation — wire Evidence intake form to use `capture="environment"` for camera + upload via Supabase Storage when configured; then Sprint 10B relational mappers for projects/cashflow/PR. |
| P1-020 | done | Codex | Project Access action enforcement | Added shared guarded evaluation in `src/projectAccess.ts`, mapped Document Authority actions to permissions, gated BuildDocs authority buttons/actions, gated Approval Center approve/reject actions, and kept local-first workspaces open until an active grant exists. Verification: `npx tsc --noEmit` pass, `npm test -- --run` 521/521 pass, `npm run build` pass with existing Vite chunk warning. |
| P1-021 | done | Codex | Project Control planner baseline hardening | Project Control now reads Construction Planner BOQ task linkage as baseline budget, reconciles unlinked planner budget back to the project total, shows Schedule status instead of misleading negative days for draft baselines, suppresses false low-margin alerts for draft baselines, and uses planned cost as forecast margin basis before actuals. Verification: Project Control tests pass 37/37, full suite 531/531, build pass with existing Vite chunk warning, Browser smoke dashboard/reports/settings pass, and `/roadmap` quality tile derives the latest test label. |
| P1-022 | done | Codex | Defect Site Report location pin board | Site Report now groups EvidenceAsset floor/room/zone/viewpoint tags into Location pins, adds pin-scoped report event filtering, scopes the project evidence list to the selected pin, and documents the non-persistent pin model. Verification: `npx tsc --noEmit` pass, full suite 531/531, `npm run build` pass with existing Vite chunk warning, Chrome CDP smoke `/defect?tab=site-report&version=0.1` + `/roadmap` pass. |
| P1-019 | done | Codex | Admin Project Access UI | Added `/admin?tab=project-access&version=0.1` tab for project-scoped role grants, supplier scope, extra/denied permissions, active/inactive/delete actions, access check result, audit writes, shell route/copy wiring, and responsive admin CSS. Verification: `npx tsc --noEmit` pass, `npm test -- --run` 514/514 pass, `npm run build` pass with existing Vite chunk warning, Browser smoke created `Site Manager` grant and access check returned Allowed. |
| P1-017 | done | Codex | Construction Planner Preview workspace app | Added `constructionPlanner` app route `/construction-planner?tab=overview&version=0.1`, built read-only parser/seed/panel for `Construction Planning Spreadsheet.xlsx`, local-first `.xlsx` import override, workbook-style overview sheet preview, Gantt/BOQ/planned curve tabs, workspace sync into Projects/BOQ Data/Project Control baseline, storage key `construction-planner.preview.v1`, parser/sync tests, docs, and roadmap updates. Verification: 521/521 tests pass, `npm run build` pass with existing Vite chunk-size warning, and preview smoke routes return 200. |
| P1-018 | done | Codex | Defect Site Report + EvidenceAsset bridge | Added `/defect?tab=site-report&version=0.1` workflow inspired by BUILK360: report event filters, copy/print PDF output, time-compare panel, deep link placeholder, manual project evidence intake for 360/file links, and floor/room/zone/viewpoint metadata. Site Report writes to the existing `src/evidence.ts` Evidence layer (`site_360` / `site_file`) via project `EvidenceLink` and `site-floor`/`site-room`/`site-zone`/`site-viewpoint` tags instead of duplicating workspace storage. Updated Roadmap, PRD, Evidence PRD, Data Dictionary, and BUILK360 analysis. Verification: 521/521 tests pass; build clean with existing Vite chunk warning. |
| P1-006 | done | Codex | Builk parity Sprint 5 — Cashflow project extension + rollup engine + recurring templates + Project Detail บันทึกต้นทุน tab | Extended `CashflowEntry` with 8 new optional fields (costCodeId/supplierId/prId/rfqId/poDocumentId/quantityActual/unitActual/recurringTemplateId) + `sourceType`/`sourceDocumentId` (Codex addition); backward compat via normalize (old records load with empty defaults). Added `src/cashflow.rollup.ts` pure functions (`computeProjectRollup`, `syncProjectFromCashflow`, `syncProjectsFromCashflow`, `filterCashflowEntries` with sourceType filter, `buildSupplierPriceHistoryEntryFromCashflow`, `syncSupplierPriceHistoryFromCashflow`, `removeCashflowPriceHistory`) — confirmed expense entries roll up to `Project.actualCost`, confirmed income to `actualRevenue`, supplier price history syncs from confirmed `supplierId+costCodeId+amount` without duplication (uses `cashflow-price-{entryId}` id convention). Added `src/cashflow.recurring.ts` with `RecurringTemplate` type + storage helpers (`loadRecurringTemplates`/`saveRecurringTemplates`/`upsertRecurringTemplate`/`removeRecurringTemplate`) + `generateEntriesFromTemplate` (iterates monthly/weekly/quarterly/yearly periods since `lastGeneratedDate` up to today, respects `endDate`+`active`, sets `recurringTemplateId` on generated entries as draft, idempotent on re-call) + `generateAllRecurring` aggregator. Tests: `src/cashflow.rollup.test.ts` 7 tests (rollup/sync/filter/price-history sync) + `src/cashflow.recurring.test.ts` 23 tests (storage/normalize/upsert+remove/generate per frequency/idempotency/endDate respect/projectId propagation/aggregator). Upgraded `CashflowPanel` form with Project/Cost Code/Supplier/actual qty/actual unit fields + list filters; replaced `filterCashflowByProject` simple helper retention with shared rich `filterCashflowEntries` in rollup module. Wired Project Detail "บันทึกต้นทุน" tab (`/projects?id=...` → cost tab) — new `ProjectCashflowTab` component shows scoped table of all cashflow entries for the project with category/cost-code columns + per-cost-code rollup summary + link-out to `/cashflow` (TH/EN). `supabase/migrations/0008_cashflow_extension.sql` ALTER cashflow_entries with 9 new columns + 5 indexes + DO-block source_type CHECK constraint + CREATE `recurring_templates` table with full RLS + 3 indexes + updated_at trigger. 381 tests pass (30 new) · build clean (workspace 519 KB gzip 128 KB) · `/cashflow` + `/projects` + `/procurement` return 200 |
| P1-004 | done | Codex | Builk parity Sprint 3 — Procurement (Purchase Request) workflow | `src/procurement.ts` data layer (PR + RFQ types in single module + 2 storage keys + auto-numbering `nextPrNumber` / `nextRfqNumber` `{PR\|RFQ}-{YYYY}-NNN` with year reset + pure-function state machine `transitionPRStatus` 10 states / 10 actions + `canTransition` / `legalActionsFor` helpers + validatePR (projectId/items/per-item costCode/unit/qty>0) + createPR + upsertPR + removePR + applyPRAction with context (approverId/reason/awardedSupplierId/linkedRfqId/linkedPoDocumentId) + filterPRs (status/project/search) + summarizePRs (total/byStatus/pendingApproval/inProgress/totalAmount) + draftRFQFromPR scaffold for Sprint 4); `src/procurement.pr.test.ts` + `src/procurement.rfq.test.ts` cover PR/RFQ storage, state transitions, comparison matrix, award flow, and summaries; new `procurement` app in `src/apps.ts` (route `/procurement`, procurement sidebar group as third member after costCodes+suppliers, taxonomy construction/freemium/write/workspace/optional AI); shell files updated (workspaceGroups + workspaceRouting subnav pr-list/rfq-list/archive + workspaceCopy TH/EN); `src/workspace/apps/procurement/ProcurementPanel.tsx` — PR list tab (8-status filter chips + project filter + search + 6-col table with status badge + create button); PR detail/form tab (header with PR No + status badge + action toolbar via `legalActionsFor()`; project dropdown + dates + notes + inline line-items table with Cost Code/Supplier linkage); Archive tab; RFQ stub. Wired into `WorkspaceAppPanel` after Suppliers; `supabase/migrations/0006_purchase_requests.sql`. 351 tests pass · build clean (workspace 507 KB gzip 128 KB) · `/procurement` + `/suppliers` + `/cost-codes` + `/projects` return 200 |
| P1-003 | done | Codex | Builk parity Sprint 2 — Suppliers directory + price history | `src/suppliers.ts` data layer (Supplier + SupplierPriceHistoryEntry combined into one SupplierState + storage via adapter + validateSupplier (name/tax_id 13 digits/email/rating 0-5) + detectSupplierType heuristic (บจก./หจก./รับเหมา/Co./Ltd) + formatThaiTaxId/stripTaxIdFormat (`0-1057-12345-67-8`) + upsertSupplier + deactivateSupplier soft + removeSupplier cascade-deletes price history + addPriceHistoryEntry + removePriceHistoryEntry + searchSuppliers (digits-only guard fixes empty-string tax match) + filterSuppliers (type/city/activeOnly) + sortSuppliers (name/spend/lastOrder/rating) + computeSupplierSummary 12-month window + recentPricesForSupplier with driftPct vs previous same-code entry + summarizeSupplierDirectory + createSeedSuppliers 5 suppliers + ensureSeedSuppliers idempotent + parseSuppliersCsv (alias headers, tax_id length check, type auto-detect, semicolon/comma tags) + applySupplierCsvImport (dedupe by tax_id else name)); `src/suppliers.test.ts` 54 tests; new `suppliers` app in `src/apps.ts` (route `/suppliers`, procurement sidebar group, taxonomy: construction/freemium/write/workspace/optional AI); `src/workspace/apps/suppliers/SuppliersPanel.tsx` — Directory tab (split-view: left list with rating stars + total spend + order count, sorted by spend/last/name/rating, type filter; right detail with header KPIs + tax id formatted display + tags + notes + edit/deactivate + inline PriceHistoryForm with cost code autocomplete from `loadCostCodes()` + recent prices table with drift % indicator + remove price entry); Price History tab (cross-supplier table sorted by date); Import tab (paste/file CSV + preview table + skip vs update toggle); Analytics stub. Empty-state offers seed action. TH/EN copy. Wired into `WorkspaceAppPanel` after CostCodes; `supabase/migrations/0005_suppliers.sql` (suppliers table + supplier_price_history table + 8 indexes + workspace-only RLS for both + updated_at trigger for suppliers). 247 tests pass · build clean (workspace 453 KB gzip 114 KB) · `/suppliers` + `/cost-codes` + `/projects` return 200 |
| P0-006 | done | Codex | Re-scope product direction from portfolio to Build By BIM Platform | `docs/PRD.md` states portfolio is only a developer page and core product is tools, content, membership, support, and services |
| P0-007 | in_progress | Codex | Write public website PRD | PUBLIC_ROUTES.md ครอบคลุม IA + นำมา implement ที่ `src/PublicSite.tsx` ครบ 9 routes พร้อม TH/EN + filter; OG/Twitter meta ใน `index.html` พร้อมแชร์ Facebook/LINE; ส่วนเหลือ: Facebook acquisition funnel (UTM convention + landing variant) รอ v0.5 |
| P0-008 | done | Codex | Write membership/support PRD | `docs/MEMBERSHIP_ACCESS_PRD.md` ครบ Section 1-9; implement ที่ `src/membership.ts` + `/account` + `/support-plans` + `/admin`; payment decision รอเลือก provider ใน v0.5 |
| P0-009 | done | Codex | Add multi-profession expansion strategy | `docs/PRD.md` states construction is the first vertical but the platform must support other professions |
| P0-010 | done | Codex | Add cross-profession collaboration strategy | `docs/PRD.md` explains construction as a bridge to connected professions and prioritizes workflow collaboration before social network features |
| P0-011 | done | Codex | Add wedge-app go-to-market strategy | `docs/PRD.md` states the first market approach is selling one practical app at a time while keeping BIM/IoT as future architecture |
| P0-012 | done | Codex | Add Agent API and internal CLI direction | `docs/PRD.md` and `docs/ARCHITECTURE.md` define Agent API, LINE/web agent flow, internal CLI scope, tool safety, and audit requirements |
| P0-013 | done | Codex | Create platform ERD and development plan | `docs/PLATFORM_ERD.md` and `docs/DEVELOPMENT_PLAN.md` document the conceptual data model and phased roadmap |
| P0-014 | done | Codex | Add instant utility and future memory principle | `docs/PRD.md` and `docs/DEVELOPMENT_PLAN.md` require each app to solve immediate pain fast and save reusable data for later |
| P0-015 | done | Codex | Add platform data strategy | `docs/DATA_STRATEGY.md` defines data ownership, structured persistence, backup/export, privacy/security, quality, and first backend cut |
| P0-016 | done | Codex | Add architect-led design workflow direction | `docs/PRD.md` and `docs/DEVELOPMENT_PLAN.md` define design apps for brief, site analysis, moodboard, prompt/render, options, presentation, and BIM handoff |

## BOQ Data

| ID | Status | Owner | Task | Acceptance |
| --- | --- | --- | --- | --- |
| BOQ-001 | done | Codex | Write Task-to-BOQ linkage PRD | Problem, goal, non-goals, data model, storage key, UI requirements, and acceptance criteria are in `docs/PRD.md` |
| BOQ-002 | done | Codex | Implement `boq-data.task-linkage.v1` storage model | Local-first task linkage loads, normalizes, saves, and survives reload without touching Docs line items |
| BOQ-003 | done | Codex | Add `Task Linkage` BOQ Data subnav | `/boq-data?tab=task-linkage&version=0.1` opens the linkage workspace |
| BOQ-004 | done | Codex | Build Task-to-BOQ linkage UI | User can create/select task, search BOQ item, assign budget, remove linkage, and see warning state |
| BOQ-005 | done | Codex | Calculate linkage summary from saved task data | UI shows allocated, remaining, over-budget, and linked item count from persisted linkage state |

## Business Finance

| ID | Status | Owner | Task | Acceptance |
| --- | --- | --- | --- | --- |
| FIN-001 | todo | unassigned | Park `debtPlanner` scope in code/UI if needed | Decide whether to hide placeholder route/card or keep it visibly parked; no debt payoff implementation |
| C-001 | done | Codex | Write `cashflow` module spec for business/project cashflow | `docs/CASHFLOW_PRD.md` covers income/expense model, draft-first flow, ERD mapping, UI flow, acceptance criteria, and v0.2+ roadmap |
| C-002 | done | Codex | Implement cashflow dashboard | Overview tab live at `/cashflow?tab=overview&version=0.1` with quick-add form (draft + confirm), entry list, monthly summary, negative cashflow warning, and Hub pending action; forecast/reports tabs scaffolded with empty states |

## Design Workflow

| ID | Status | Owner | Task | Acceptance |
| --- | --- | --- | --- | --- |
| D-001 | todo | unassigned | Write Design Brief PRD | Define client need, project type, room/space requirement, style preference, site condition, budget, constraints, and save/export behavior |
| D-002 | todo | unassigned | Write Moodboard PRD | Define reference files, material/color palette, style tags, prompt notes, source tracking, and design option links |
| D-003 | todo | unassigned | Write Design Options PRD | Define option comparison, feedback, approval status, cost/design risk notes, and handoff to Docs/BOQ/BIM |
| D-004 | todo | unassigned | Write Presentation Builder PRD | Define presentation sections, concept images, notes, estimate summary, PDF/export, and client approval flow |
| D-005 | todo | unassigned | Define design data contract | Data contract covers brief, site observation, style/material palette, prompt version, generated image, option, feedback, and approval |
| D-006 | done | Codex | Write Design Plan Review / Architect Brain PRD | `docs/DESIGN_PLAN_REVIEW_PRD.md` defines brief/plan upload, multi-layer review, outputs, data model, handoffs, non-goals, and MVP scope |
| D-007 | todo | unassigned | Decide Architect Brain integration approach | Choose import KG/files, API bridge to `arch-brain-web`, or rebuild the core review engine as a module in this app |
| D-008 | todo | unassigned | Prototype Plan Review app shell | `/design/plan-review` has quick mode, saved mode, upload/input area, structured findings, score, action list, and export/copy summary |

## Growth, Membership And Revenue

| ID | Status | Owner | Task | Acceptance |
| --- | --- | --- | --- | --- |
| G-001 | done | Codex | Define Build by BIM public information architecture | `docs/PUBLIC_ROUTES.md` defines `/`, `/apps`, `/tools`, `/prompts`, `/workflows`, `/pricing`, `/developer`; developer page is secondary |
| G-002 | todo | unassigned | Design content-to-tool funnel | Facebook/social content links into free tools/templates with clear signup and upgrade points |
| G-003 | done | Codex | Define social sharing metadata | `index.html` มี OG title/description/image/locale/url/canonical + Twitter card; preview image `/og-cover.png` ใน publish root (ยังต้องเพิ่มไฟล์รูปก่อน prod); UTM convention รอ Facebook content workflow รอบหน้า |
| G-004 | done | Codex | Define multi-profession app taxonomy | `docs/APP_TAXONOMY.md` covers category, profession tag, difficulty, free/paid access, privacy, AI usage, persistence, and recommended pack metadata |
| G-005 | todo | unassigned | Choose first non-construction profession pack | Pick one target profession/use case with clear free tool, paid upgrade, and support offer |
| G-006 | todo | unassigned | Define cross-profession collaboration PRD | Specify request/response, shared document, project handoff, role permissions, and external sharing scope |
| G-007 | todo | unassigned | Choose first collaboration workflow | Pick one workflow such as quote request, document share, review request, support request, or project handoff |
| G-008 | todo | unassigned | Choose first wedge app for Thai market validation | Pick one of BOQ Data, BuildDocs, Defect, Site Report, or AI Document Assistant with a clear pain, content angle, and paid upgrade |
| G-009 | todo | unassigned | Define wedge app landing page and pricing | Landing page avoids BIM/IoT jargon and focuses on time saved, fewer errors, clearer documents, and support offer |
| G-010 | todo | unassigned | Define market education content plan | Facebook/social content explains practical workflow problems before introducing BIM-ready or IoT-ready concepts |
| G-011 | todo | unassigned | Define quick mode and saved mode for first wedge app | User can get first output quickly without setup, then save to workspace for reuse after signup/login |
| G-012 | done | Codex | Define Tools Apps taxonomy | `docs/APP_TAXONOMY.md` covers planner, calculator, checklist, generator, tracker, assistant, converter, extractor, privacy level, and monetization options |
| G-013 | done | Codex | Write Facebook Content Workflow PRD | `docs/FACEBOOK_CONTENT_WORKFLOW_PRD.md` defines topic-to-post workflow, quick/saved mode, data model, approval rules, MVP scope, and non-goals |
| G-014 | todo | unassigned | Prototype Facebook Content Workflow app shell | `/workflows/facebook-content` creates post draft options with hook, caption, CTA, hashtag, image prompt, approval state, and copy/export |
| G-015 | done | Codex | Write Prompt Set Library PRD | `docs/PROMPT_SET_LIBRARY_PRD.md` defines prompt sets, variables, sharing/access, data model, safety, MVP scope, and handoff targets |
| G-016 | todo | unassigned | Prototype Prompt Set Library app shell | `/prompts` shows prompt set catalog, detail page, variable form, final prompt generator, copy, favorite, access label, and example output |
| G-017 | done | Codex | Prototype public route shells | `/` ใช้ PublicStylePreview (landing เดิม) · `/apps`, `/tools`, `/prompts`, `/workflows`, `/pricing`, `/developer` มี shell + nav + footer พร้อม TH/EN; `/apps` ดึง catalog จาก `src/apps.ts` แบบ live filter ตาม `WorkspaceAppStatus`; ส่วนอื่นเป็น static stub พร้อม PRD reference; workspace hub ย้ายไป `/hub` |
| M-001 | done | Codex | Define membership data model | `src/membership.ts` มี `Plan`, `Subscription`, `AppAccessRule`, `AppAccessOverride`, `AuditLogEntry` + storage helpers + `evaluateAppAccess()` ตาม MEMBERSHIP_ACCESS_PRD.md Section 5 (override > plan > default free); `SupportRequest`/`Payment` รอ v0.5 backend |
| M-002 | done | Codex | Define configurable support tiers | seed 3 plans (Free / Support Monthly / Custom) ผ่าน `seedPlans()` + AppAccessRule ครบทุก app; admin สามารถเรียก `upsertPlan()`/`upsertAppAccessRule()` ปรับได้; UI ที่ `/support-plans` แสดง access matrix + ปุ่ม Activate |
| M-003 | todo | unassigned | Decide auth/backend/payment architecture | Provider choices and tradeoffs recorded in `docs/DECISIONS.md` before coding |
| M-004 | todo | unassigned | Define collaborator role/access model | Roles cover owner, member, collaborator, reviewer, vendor, support operator, and read-only external viewer |
| M-005 | done | Codex | Prototype admin access override matrix | `/admin?tab=overrides` รับ scope (workspace/member/user) × app × effect (allow/deny) × accessLevel × reason + บันทึก audit ทุกครั้ง; `/admin?tab=audit` แสดง read-only feed; access gate ใน workspace ตรวจ `evaluateAppAccess()` ตาม PRD Section 5 — ถ้า preview/none/deny แสดง upgrade screen พร้อมปุ่มไป `/support-plans` หรือ `/admin` |

## Agent, LINE And Automation

| ID | Status | Owner | Task | Acceptance |
| --- | --- | --- | --- | --- |
| AG-001 | todo | unassigned | Write Agent Control Layer PRD | Define agent runs, messages, inbox items, tool registry, permission checks, confirmations, audit logs, and route ownership |
| AG-002 | todo | unassigned | Define safe tool registry | Initial tools cover read, draft, write, search, and handoff classes with allowlist names and required roles |
| AG-003 | todo | unassigned | Define LINE receipt intake flow | Flow covers webhook, message content download, private file storage, OCR/extraction, user confirmation, and expense/document/cashflow save |
| AG-004 | todo | unassigned | Define internal Agent CLI contract | CLI calls admin Agent API/service functions and cannot expose arbitrary shell execution to web users |
| AG-005 | todo | unassigned | Define agent audit schema | `agent_runs`, `agent_actions`, `agent_tool_calls`, and `audit_logs` capture who/what/when/source/result |

## BuildDocs And Operations

| ID | Status | Owner | Task | Acceptance |
| --- | --- | --- | --- | --- |
| B-001 | todo | unassigned | Validate document relationship flows | Quote/contract to billing, invoice to receipt, status transitions tested manually |
| B-002 | todo | unassigned | Improve backup/import resilience | Import handles old/current schemas and reports clear errors |
| B-003 | todo | unassigned | Review legal text caveat for contract templates | UI/docs clearly say contract drafts require legal review |
| Q-001 | todo | unassigned | Audit Thai text encoding | README/source/docs display Thai correctly in editor/browser/build output |

## Architecture Backlog

| ID | Status | Owner | Task | Acceptance |
| --- | --- | --- | --- | --- |
| A-001 | todo | unassigned | Decide whether to migrate `build-by-bim.app-version-selection.v1` key | Keep with documented reason or migrate with compatibility |
| A-002 | done | Codex | Plan backend sync | `src/storageAdapter.ts` ครบ (StorageAdapter interface + LocalStorageAdapter + MemoryAdapter + swappable singleton + readJson/writeJson); refactor `cashflow.ts` + `membership.ts` + `boqTaskLinkage.ts` + `stylePreviewI18n.ts` ใช้ adapter ครบทุก module data layer; พร้อมเพิ่ม Supabase/REST adapter แค่ implement interface + setStorageAdapter() — ไม่ต้องแก้ business logic |
| A-003 | todo | unassigned | Plan image/photo storage beyond localStorage | Avoid quota failures for defect photos and library assets |
| A-004 | todo | unassigned | Add private Google Sheets integration plan | OAuth/backend approach documented before coding |
| A-005 | todo | unassigned | Add PDF generation plan | Decide browser print vs server/client PDF engine and acceptance criteria |
| A-006 | todo | unassigned | Convert conceptual ERD into first Supabase schema cut | SQL migration covers first production tables from `docs/PLATFORM_ERD.md` with RLS assumptions |
| A-007 | todo | unassigned | Decide first 30-day execution path | Confirm wedge app, domain, backend timing, payment approach, and first LINE agent use case from `docs/DEVELOPMENT_PLAN.md` |
| A-008 | todo | unassigned | Define data contract for first wedge app | Contract covers workspace scope, tables/storage keys, import/export, backup, permissions, audit events, and app links from `docs/DATA_STRATEGY.md` |
| A-009 | todo | unassigned | Define backup/export MVP | JSON workspace export, PDF document export, CSV BOQ/expense export, file download, and restore expectations are specified |

## Done Log

- 2026-05-25: **Site Report capture sets + same-pin compare shipped.** Added local-first capture set storage in `src/workspace/apps/defects/siteReportService.ts` with `site-report.capture-sets.v1`, normalization, project/plan filtering, upsert helpers, EvidenceAsset reserved tag helpers (`site-plan:*`, `site-pin:*`, `site-capture:*`), and plan-pin/capture filters. Wired `/defect?tab=site-report&version=0.1` with a Capture sets board, plan-pin/capture selectors on Evidence intake, tagged evidence creation, and same-pin reference/current compare. Updated PRD, Site Report PRD, Data Dictionary, Sync Contract, Architecture, BUILK360 analysis, Roadmap, and TASKS; next Site Report slice is comments/comment-to-task. Verification: focused Site Report/Defect tests passed 16/16, `npx tsc --noEmit` passed, `npm test -- --run` passed 597/597, `npm run build` passed with the existing Vite chunk-size warning, HTTP smoke returned 200 for `/defect?tab=site-report&version=0.1` and `/roadmap`, and Browser smoke confirmed one plan, one pin, two capture sets, two tagged evidence rows, same-pin compare `reference 1 / current 1`, and `/roadmap` v0.4.39.

- 2026-05-25: **Project Control source CSV helper tests shipped.** Moved the source-detail CSV headers and row flattener out of `ProjectControlPanel.tsx` into `src/projectControl.ts`, where they can be reused and tested without rendering the dashboard. Added 3 unit tests covering stable header order, Construction Planner source-link flattening, and empty-source omission. Updated PRD, Roadmap, and TASKS. Verification: focused `projectControl` tests passed 41/41, `npx tsc --noEmit` passed, `npm test -- --run` passed 597/597, `npm run build` passed with the existing Vite chunk-size warning, HTTP smoke returned 200 for `/project-control?tab=dashboard&version=0.1` and `/roadmap`, and Browser smoke confirmed source CSV still visible/enabled plus `/roadmap` v0.4.38.

- 2026-05-25: **Project Access member directory shipped.** Added `project-access.members.v1` in `src/projectAccess.ts` with seed members, normalization, load/save/upsert helpers, active-member actor listing, and a same-tab update event. Admin Project Access now has a member directory board and member picker; saving a grant also upserts the member record. The workspace topbar now reads active members from the directory and uses `Member`/`ผู้ใช้งาน` copy instead of a hardcoded test-actor list. Updated PRD, Architecture, Data Dictionary, Sync Contract, Roadmap, and TASKS. Verification: `npx tsc --noEmit` passed, focused `projectAccess` tests passed 25/25, full suite passed 594/594, `npm run build` passed with the existing Vite chunk-size warning, and Browser smoke confirmed `/admin?tab=project-access&version=0.1`, `/procurement?tab=pr-list&version=0.1`, and `/roadmap`.

- 2026-05-25: **Project Control source CSV export shipped.** Added a separate dashboard export button that flattens Construction Planner source links from the selected Project Control snapshot into a CSV audit file with generated_at, project, cost code, task, BOQ item, amount, and source columns. The original rollup CSV export remains unchanged, and the source export is disabled when no source rows exist. Updated PRD, Architecture, Roadmap, and TASKS. Verification: `npx tsc --noEmit` passed, `npm test -- --run` passed 594/594, `npm run build` passed with the existing Vite chunk-size warning, HTTP smoke returned 200 for `/project-control?tab=dashboard&version=0.1` and `/roadmap`, and Browser smoke confirmed the source CSV button is visible/enabled plus `/roadmap` v0.4.36.
- 2026-05-25: **Site Report Plan Pin board Phase 1 shipped.** Added local-first Site Report plan/pin storage helpers in `src/workspace/apps/defects/siteReportService.ts` with `site-report.plans.v1` and `site-report.pins.v1`, normalization, project/plan filtering, Location pin binding, x/y clamping, upsert helpers, and next-pin numbering. Wired `/defect?tab=site-report&version=0.1` with plan creation, numbered pins on the plan canvas, a pin inspector/list, and selection that scopes the existing Site Report event/evidence view through the bound Location pin. Updated PRD, Site Report PRD, Data Dictionary, Sync Contract, Architecture, BUILK360 analysis, Roadmap, and TASKS; next Site Report slice is capture sets + same-pin compare. Verification: `npx tsc --noEmit` passed, `npm test -- --run` passed 587/587, `npm run build` passed with the existing Vite chunk-size warning, HTTP smoke returned 200 for `/defect?tab=site-report&version=0.1` and `/roadmap`, and Chrome CDP smoke confirmed plan/evidence/pin creation, active Location pin filtering, `/roadmap` v0.4.32, and no console/network errors.
- 2026-05-25: **Project Access actor switcher shipped.** Added `project-access.local-actor.v1`, `PROJECT_ACCESS_TEST_ACTORS`, and local actor load/save helpers in `src/projectAccess.ts`; added a workspace topbar selector so testers can switch between Site Manager, Project Owner, Procurement, Accounting, Reviewer, Viewer, Vendor, and Workspace Admin. Projects, Procurement, Cashflow, Project Control, Approval Center, and BuildDocs now evaluate guards against the selected local actor. Verification: `npx tsc --noEmit` passed, focused tests passed 27/27, full suite passed 587/587, and `npm run build` passed with the existing Vite chunk-size warning.
- 2026-05-25: **Project Control source detail + BOQ route focus hardening shipped.** Project Control cost breakdown source blocks now expose an expandable list of every Construction Planner BOQ source row with task, BOQ code, amount, and direct open action. BOQ Data Task Linkage now keeps the route-target BOQ row pinned in the picker even when the active search/filter would hide it, and the bad `ยท` separator in task-linkage cost-code labels was corrected to `·`. Also fixed an Approval Center ternary typo from the local Project Access actor refactor that blocked typecheck. Verification: `npx tsc --noEmit` passed, `npm test` passed 587/587, and `npm run build` passed with the existing Vite chunk-size warning.
- 2026-05-24: **Project Access financial/control enforcement shipped.** Added shared local Project Access helpers in `src/projectAccess.ts` plus a compact `ProjectAccessNotice` component. Wired `/procurement` to scope PR/RFQ list/detail/archive views by `procurement.read` and guard PR/RFQ write, delete, send, response, approve, and award paths with `procurement.write`/`procurement.approve`. Wired `/cashflow` to scope visible projects/entries by `cashflow.read` and guard draft, confirm, and delete with `cashflow.write`/`cashflow.approve`. Wired `/project-control` dashboard/reports to scoped project, PR, cashflow, and BOQ task contexts by `report.read`. Verification: `npx tsc --noEmit` passed, `npm test -- --run` passed 536/536, and `npm run build` passed with the existing Vite chunk-size warning.
- 2026-05-24: **Site Report 360 coordination PRD ready.** Added `docs/SITE_REPORT_360_PRD.md` from the BUILK360 product/manual study as a clean-room product spec for Plan Pin board, capture sets, same-pin time compare, comments, comment-to-task, File Center, report templates, storage keys, permissions, acceptance criteria, and phased delivery. Linked the PRD from Master PRD, `docs/BUILK360_SITE_REPORT_ANALYSIS.md`, TASKS, and Roadmap so the next implementation slice is clear: Site Report Plan Pin board Phase 1. Verification: `npm test -- --run` passed 536/536, `npx tsc --noEmit` passed, `npm run build` passed with the existing Vite chunk-size warning, and route smoke returned 200 for `/roadmap` plus `/defect?tab=site-report&version=0.1`.
- 2026-05-24: **Project Control BOQ source drill-down shipped.** `computeProjectSnapshot` now preserves Construction Planner task/BOQ source links on cost-code rollups, and the Project Control dashboard exposes BOQ source buttons inside cost breakdown rows. BOQ Data Task Linkage now reads `projectId`, `taskId`, `boqItemId`, and `costCode` from the route query, selects the matching task, highlights the linked BOQ row, and shows an "Opened from Project Control" focus banner. Also fixed the ProjectsPanel `WorkspaceLanguage` import so typecheck/build can pass. Verification: focused Project Control tests passed 38/38, `npx tsc --noEmit` passed, full suite passed 536/536, `npm run build` passed with the existing Vite chunk-size warning, Browser smoke confirmed Project Control -> BOQ Data drill-down and `/roadmap` v0.4.25 with 536/536.

- 2026-05-24: **Roadmap module split shipped.** Extracted `/roadmap` UI and page-specific CSS from `src/PublicSite.tsx` into `src/public/RoadmapPage.tsx`; `PublicSite.tsx` now keeps route wiring only. Updated `src/roadmap.ts` to v0.4.20 and synced PRD/TASKS so Roadmap remains part of the completion rule. Verification: `npx tsc --noEmit` passed, `npm test -- --run` passed 531/531, `npm run build` passed with the existing Vite chunk-size warning, built bundle includes `v0.4.20`, and HTTP smoke returned 200 for `/roadmap`, `/apps`, and `/`.
- 2026-05-24: **Project Access Projects/export enforcement shipped.** Extended Admin Project Access into user-facing project surfaces: `/projects` now filters list/detail/analytics to readable grants, blocks direct inaccessible project URLs, and gates create/edit/delete with `project.write`/`project.admin`; BuildDocs print/PDF, share/send, and workspace backup export now use `document.export`/`settings.manage` guards with disabled buttons plus runtime checks. Added project access tests for denied read visibility and workspace export permissions. Verification: `npx tsc --noEmit` passed, `npm test -- --run` passed 531/531, `npm run build` passed with the existing Vite chunk-size warning, and Browser smoke covered `/projects`, `/docs`, and `/roadmap`.
- 2026-05-24: **Defect Site Report location pin board shipped.** Added Site Report Location pins derived from EvidenceAsset location tags (`site-floor`, `site-room`, `site-zone`, `site-viewpoint`, with legacy `location:` fallback). Selecting a pin now scopes report events and the project evidence list to the same site point, while the default view still shows all locations. Updated Roadmap, PRD, Evidence PRD, Data Dictionary, and BUILK360 analysis to reflect the non-persistent pin model. Verification: `npx tsc --noEmit` passed, `npm test -- --run` passed 531/531, `npm run build` passed with the existing Vite chunk-size warning, and Chrome CDP smoke confirmed adding `VP-01` evidence creates a Location pin, pin filtering works, `/roadmap` shows `v0.4.19` + `531/531`, and no console/network issues were detected.
- 2026-05-24: **Project Control planner baseline hardening shipped.** Fixed the Project Control dashboard after Construction Planner sync so it is usable as a real review surface: `computeProjectSnapshot` now reads `boq-data.task-linkage.v1` planner task allocations as baseline budget, adds `planner-unlinked` budget to reconcile linked BOQ totals back to the project total, uses planned cost as the forecast margin basis before actuals, and suppresses low-margin alerts for draft baselines. The dashboard now shows `Schedule` as project status for draft imported plans instead of negative days. Also fixed the `replaceAll` build-target issue in `buildDocsCopy.ts` and removed the stale hardcoded Roadmap quality count. Verification: `src/projectControl.test.ts` passed 37/37, full suite passed 531/531, `npm run build` passed with the existing Vite chunk-size warning, Browser smoke confirmed `/project-control?tab=dashboard` shows full budget `฿2,169,751`, planner baseline/unlinked cost rows, no low-margin alert, plus `/project-control?tab=reports`, `/project-control?tab=settings`, and `/roadmap` render without console errors.
- 2026-05-24: **Project Access action enforcement shipped.** Added `evaluateProjectAccessGuard()` and Document Authority permission mapping in `src/projectAccess.ts`, then wired BuildDocs authority actions and Approval Center approve/reject buttons to the same Admin Project Access grants. Empty local-first workspaces remain open until a first active grant exists; once grants exist, mismatched/no-permission decisions block the action and show a Project Access status. Also fixed the Construction Planner sync prop/copy type blocker so full typecheck/build can pass. Verification: `npx tsc --noEmit` passed, `npm test -- --run` passed 521/521, `npm run build` passed with the existing Vite chunk-size warning.
- 2026-05-24: **Construction Planner workspace sync shipped.** Added `constructionPlannerIntegration.ts` so `/construction-planner?tab=overview&version=0.1` can sync the current workbook preview into existing local-first apps: upserts a stable Project baseline in `projects.list.v1`, writes planner schedule tasks into `boq-data.task-linkage.v1`, imports workbook BOQ rows into the custom BOQ catalog, and preserves formula-based task links such as `BOQ!N9`. Added the overview integration strip with Sync/Open Projects/Open BOQ Data/Open Project Control actions. Updated PRD, Architecture, TASKS, and Roadmap. Verification: focused integration tests passed 4/4, full suite passed 521/521, and `npm run build` passed with the existing Vite chunk-size warning.
- 2026-05-24: **Defect Site Report location metadata shipped.** Extended the Site Report Evidence intake in `/defect?tab=site-report&version=0.1` with separate floor, room, zone, and viewpoint fields. `defectService` now stores them as structured EvidenceAsset tags (`site-floor`, `site-room`, `site-zone`, `site-viewpoint`) while keeping legacy `location:` tag fallback, and report events render the formatted location string for future 360 pins. Updated Roadmap, PRD, Evidence PRD, Data Dictionary, and BUILK360 analysis. Verification: focused defectService tests passed 7/7, full suite passed 521/521, `npm run build` passed with the existing Vite chunk-size warning, and route smoke for `/defect?tab=site-report&version=0.1` plus `/roadmap` recorded.
- 2026-05-24: **Admin Project Access UI shipped.** Added `/admin?tab=project-access&version=0.1` as the first management surface for Builk-style user levels and project-scoped permissions. The tab can create/update/delete grants with project role, optional supplier scope, extra permissions, denied permissions, active state, and an access-check result using the shared `src/projectAccess.ts` evaluator. Shell route/copy now includes `project-access`, Admin UI writes membership audit entries for grant changes, and responsive CSS keeps the permission matrix/table usable on desktop and mobile widths. Verification: `npx tsc --noEmit` passed, `npm test -- --run` passed 514/514, `npm run build` passed with the existing Vite chunk-size warning, and Browser smoke created the default `Site Manager` grant then confirmed `document.approve` returned Allowed on `/admin?tab=project-access&version=0.1`.
- 2026-05-24: **Construction Planner workbook-style preview pass shipped.** Reworked `/construction-planner?tab=overview&version=0.1` from dashboard cards into an Excel-like sheet preview matching the source workbook direction: header/meta rows, left schedule table, right Gantt grid with orange task bars, blue phase/dependency line, green planned curve, red progress line, signature area, and weekly planned-value table. The drill-down `schedule`, `boq`, and `curve` tabs remain available. Verification: `npm test` passed 514/514, `npm run build` passed with existing Vite chunk-size warning, and preview smoke returned 200 for all Construction Planner tabs plus `/apps` and `/roadmap`.
- 2026-05-24: **Defect Site Report + EvidenceAsset bridge shipped.** Analyzed the BUILK360 site report workflow and added `docs/BUILK360_SITE_REPORT_ANALYSIS.md`. Added the Defect `Site Report` tab at `/defect?tab=site-report&version=0.1` with report event filters, summary tiles, copy report text, print/PDF action, before/after time compare, report deep link placeholder, and manual evidence intake for 360/PDF/video/file links. Extended `src/evidence.ts` with `site_360` and `site_file` asset types, then wired Site Report to the existing Evidence layer with project `EvidenceLink` records instead of creating a duplicate workspace storage model. Updated `defectService` report event builders, storage/workspace tests, shell route tests, PRD, Evidence PRD, Data Dictionary, and Roadmap. Verification: full suite 514/514 passed, `npm run build` passed with the existing Vite chunk-size warning, and route smoke for `/defect?tab=site-report&version=0.1` plus `/roadmap` recorded.
- 2026-05-24: **Construction Planner Preview shipped.** Added `constructionPlanner` to manifest, sidebar/app catalog, route parsing, app copy, and workspace panel rendering. Created `src/workspace/apps/construction-planner/` with workbook parser/normalizer for sheet `แผนงานก่อสร้าง` + `BOQ`, built-in seed extracted from `Construction Planning Spreadsheet.xlsx`, local-first import preview storage key `construction-planner.preview.v1`, overview/schedule/BOQ/curve tabs, responsive read-only Gantt/table/curve UI, and focused parser tests for Thai workbook matrix, Buddhist-year conversion, formulas, storage, invalid workbook, and seed counts. Verification: `npm test` passed 514/514, `npm run build` passed with existing Vite chunk warning, and preview smoke returned 200 for `/construction-planner?tab={overview,schedule,boq,curve}&version=0.1`, `/apps`, and `/roadmap`.
- 2026-05-24: **Approval Center document authority integration shipped.** Added BuildDocs PO/Invoice sync to `src/approvals.ts`, mapped `sent` documents into submitted approvals, and mirrored approved/cancelled source document states. Added `applyApprovalDecisionToDocumentAuthorityState()` in `src/projectAccess.ts` so Approval Center approve/reject writes approved/void authority stamps with `approvalRequestId`. Wired `ApprovalCenterPanel` to update parent document state, open source BuildDocs documents directly, and persist the shared document authority state. Updated PRD, Roadmap, Approval PRD, and task board so the next authority work is Admin/Projects access UI plus relational mapper. Verification: focused approvals/projectAccess tests 34/34 passed, full suite 505/505 passed, `npx tsc --noEmit` passed, `npm run build` passed with existing Vite chunk-size warning, and Browser smoke passed for BuildDocs PO `sent` -> Approval Center inbox -> approve -> BuildDocs status `approved` + authority approver stamp.
- 2026-05-24: **Roadmap list layout update shipped.** Simplified `/roadmap` visual design from card-heavy panels into a compact list layout: status strip, milestone timeline rows, focus list, usable-app list, and update-rule list. Updated `src/roadmap.ts` current focus to reflect the readability pass. Verification: `npm test -- --run` passed 495/495 tests, `npm run build` passed with the existing Vite chunk-size warning, and route smoke `/roadmap` returned 200.

- 2026-05-24: **BuildDocs Document Authority UI wiring shipped.** Wired `src/projectAccess.ts` into BuildDocs so every active document gets a document authority record, visible prepared/submitted/checked/approved/issued stamps, legal action buttons, audit writes, status sync to document state, preview/PDF stamp grid, and print-safe CSS. Updated PRD, Roadmap, Approval PRD, and task board so the next authority work is Approval Center linkage and Admin/Projects access UI. Verification: `npx tsc --noEmit` passed, `npm test -- --run` passed 495/495 tests, `npm run build` passed with existing Vite chunk-size warning, and Browser smoke `/docs?tab=documents&version=0.1` passed for `ส่งตรวจ -> ตรวจ -> อนุมัติ -> ออกเอกสาร` with panel/preview stamps visible.
- 2026-05-24: **Project RBAC + Document Authority foundation shipped.** Added `src/projectAccess.ts` with project-scoped grants, role permission matrix, supplier scoping, allow/deny overrides, accessible-project listing, document authority records, status transitions (`draft/submitted/checked/approved/issued/void`), actor stamps, approval request linkage, and audit writes for authority actions. Added `src/projectAccess.test.ts` covering storage, normalization, workspace owner/admin bypass, project-manager/vendor permissions, upsert/remove, document authority lifecycle, illegal transitions, voiding, and audit output. Added `supabase/migrations/0012_project_access_document_authority.sql` for `project_access_grants` and `document_authority` with RLS. Updated ERD, Data Dictionary, Sync Contract, and master PRD so the next UI work can wire BuildDocs/Approval Center/Admin to the new layer. Verification: focused projectAccess tests 12/12 passed, full suite 493/493 passed, `npx tsc --noEmit` passed, and `npm run build` passed with existing Vite chunk-size warning.
- 2026-05-24: **Public Roadmap page shipped.** Added `src/roadmap.ts` as the user-facing roadmap data source and wired `/roadmap` into `src/PublicSite.tsx` + public nav. Updated `AGENTS.md`, `docs/AGENT_WORKFLOW.md`, `docs/PUBLIC_ROUTES.md`, `docs/PRD.md`, and this task board so every completed development task must update Roadmap when feature/app/route/test/build status changes. Verification: `npm test -- --run` passed 479/479 tests, `npm run build` passed with the existing Vite chunk-size warning, route smoke for `/roadmap` returned 200.

- 2026-05-24: **RFQ award evidence gate shipped.** Extended the default `evidence.approval-policy.v1` target types to include `rfq_award`; Procurement RFQ detail now displays Evidence Gate status, opens the Evidence app, and checks the saved policy immediately before `awardRFQ`. High-value RFQ awards can now block or warn when no direct verified evidence is linked to the RFQ. Updated Evidence PRD, Procurement PRD, Data Dictionary, Sync Contract, Workflow Sequence, PRD, and task board. Verification: focused evidence/RFQ tests 64/64 passed, full suite 478/478 passed, `npx tsc --noEmit` passed, and `npm run build` passed with existing Vite chunk-size warning.
- 2026-05-24: **Configurable approval evidence gate shipped.** Added `evidence.approval-policy.v1` in `src/evidence.ts` with `off/warn/block`, `minimumAmount`, target-type selection, storage helpers, normalization, and policy-decision evaluator. Approval Center Rules tab now edits/saves the gate; Inbox blocks or warns based on the saved policy before approval. Updated Evidence PRD, Data Dictionary, Sync Contract, Workflow Sequence, PRD, and task board. Verification: focused evidence/approval tests 37/37 passed, full suite 477/477 passed, `npx tsc --noEmit` passed, and `npm run build` passed with existing Vite chunk-size warning.
- 2026-05-24: **Evidence approval soft guard shipped.** Added `buildEvidenceTargetsForApproval()` and `evaluateEvidenceRequirement()` in `src/evidence.ts`; Approval Center now loads evidence state, shows a verified/needs-review/missing evidence chip per approval row, links users to `/evidence`, and asks for confirmation before approving a transaction without direct verified evidence. Updated Evidence PRD, workflow sequence, master PRD, and task board. Verification: focused evidence/approval tests 34/34 passed, full suite 468/468 passed, `npx tsc --noEmit` passed, and `npm run build` passed with existing Vite chunk-size warning.
- 2026-05-24: **Evidence Asset Layer shipped.** Added `src/evidence.ts` local-first proof data layer with normalization, validation, file intake, status workflow, polymorphic links, filters, summaries, and CSV-ready rows; added `/evidence` workspace app (`EvidencePanel`) with Library/Intake/Links/Reports tabs; wired app manifest/sidebar/subnav/copy/render path; added `supabase/migrations/0011_evidence_assets.sql`; updated `docs/EVIDENCE_ASSET_PRD.md`, `docs/PRD.md`, `docs/IMPLEMENTED_ERD.md`, `docs/DATA_DICTIONARY.md`, `docs/WORKFLOW_SEQUENCE.md`, and `docs/SUPABASE_SYNC_CONTRACT.md`. Verification: focused tests 20/20 passed, full suite 466/466 passed, `npm run build` passed with existing Vite chunk-size warning, HTTP smoke `/evidence?tab=library&version=0.1` returned 200, in-app browser DOM smoke for Library/Intake passed with no console errors.
- 2026-05-24: **ERP handoff docs after Implemented ERD shipped.** Added `docs/DATA_DICTIONARY.md` (implemented tables/storage keys/enums/gaps), `docs/WORKFLOW_SEQUENCE.md` (Project -> PR -> Approval -> RFQ -> Cashflow -> Project Control sequences + audit points), and `docs/SUPABASE_SYNC_CONTRACT.md` (Phase D mapper contract from local keys/`kv_store` to relational Supabase tables). Updated `docs/PRD.md` file references and next-build priorities so the next concrete implementation is Evidence Asset Layer, followed by Phase D relational mappers; updated `docs/IMPLEMENTED_ERD.md` notes to point to the new handoff docs. Verification: docs-only change, inspected storage keys/migrations/source modules; build not run.

- 2026-05-24: **Builk Parity Sprint 6 — Project Control Dashboard + 5 standard reports shipped (Pojjaman-grade north star next move per `docs/POJJAMAN_GAP_ROADMAP.md`).** Created `src/csvExport.ts` shared utility — `escapeCsvValue` RFC 4180-compliant (quote fields with comma/quote/newline, double-quote escape for embedded quotes), `rowsToCsv(headers, rows[], { includeBom })` with UTF-8 BOM `﻿` prefix by default so Excel opens Thai characters without import-with-encoding wizard, CRLF line endings, `downloadCsv` browser file download helper, `dateStampForFilename` for dated names. Built `src/projectControl.ts` pure read-only aggregation (no new persisted entities except per-workspace settings, all queries are JOINs over Sprint 0-5 data): `computeProjectSnapshot(ctx)` indexes cost codes by **both** `CostCode.id` and `CostCode.code` to handle the cross-module convention split (Cashflow stores costCodeId as id, PR line items store as code), groups confirmed cashflow expense by cost code → actual, approved/rfq_sent/awarded/ordered/received PRs → committed, budget = committed when PR exists else proportional split of project.plannedCost from total actual share; produces `costCodeRollups[]` with `budget/committed/actual/remaining/variance/spentPct/isOverBudget` sorted by spentPct desc + `totalBudget/Committed/Actual/Revenue/PaidRevenue/netCashflow/marginPct/daysRemaining`. `generateAlerts` emits 7 alert types: over_budget critical when actual > budget per code, near_budget warn at >= 85% threshold (customizable), overdue critical when daysRemaining < 0 AND status in {normal, delayed}, pending_invoice warn when committed > 0 but paid revenue = 0, stale_pr warn when PR submitted > 30 days, low_margin warn at < 10% (critical at < 0), no_recent_activity info when no cashflow in 14d for normal project — all thresholds tunable via settings — sorted critical→warn→info. 5 report generators: `generateProjectPL` (row per project: planned_revenue / actual_revenue / actual_cost / profit / margin_pct), `generateCashflowForecast` (90-day window bucketed by YYYY-MM with forecast_income/expense/net), `generateCostVariance` (row per (project, cost_code) with budget/committed/actual/remaining/spent_pct/is_over_budget sorted by spent% desc), `generateSupplierSpend` (last 12 months top 10 by spend, joined to supplier name/rating), `generatePRAging` (excludes terminal status closed/cancelled, filters by age >= threshold, sorted by age desc) + `generateReport(type, projectId, ctx, options)` dispatcher (Codex addition for cleaner UI integration); `summarizeWorkspaceControl` for Hub Dashboard tile use (projectsOverBudget / projectsLowMargin / stalePRs / totalActualCost / totalPaidRevenue); ProjectControlSettings storage + DEFAULT_ALERT_THRESHOLDS + normalize. **Tests**: `src/projectControl.test.ts` 30 tests covering settings round-trip + normalize defaults / snapshot 5 cases (confirmed-only, committed-PR-filter, project-scoping, null-margin internal, order-by-spent-desc, isOverBudget flag) / alerts 7 type cases + custom threshold respect + severity sort / 5 reports happy + filter + edge cases / workspace summary; `src/csvExport.test.ts` 14 tests (escape per RFC 4180, BOM prefix + opt-out, CRLF line endings, Thai chars, boolean serialize, headerOrder respect, missing fields render empty). UI: added `projectControl` to `src/apps.ts` (route `/project-control`, taxonomy construction/freemium/read/workspace/optional AI) + shell wiring (LineChart icon, procurement sidebar group as fourth member after costCodes+suppliers+procurement, subnav dashboard/reports/settings, TH/EN workspaceCopy). Built `src/workspace/apps/project-control/ProjectControlPanel.tsx` — workspace-level 4 summary tiles (Projects total / Over budget count / Low margin count / Stale PR count); **Dashboard tab**: project picker dropdown + 7 KPI tiles (Budget/Committed/Actual/Remaining/PaidRevenue/Net/Margin%) + alert banner sorted critical→warn→info with action deep-links + per-cost-code breakdown bars (color: green <85%, yellow 85-100%, red >100% with row background tint) + AI insights dark card placeholder for Phase 2 + manual refresh button; **Reports tab**: scope dropdown (all projects / per-project) + 5 report cards as clickable buttons → on click render result table with CSV download (filename `{type}-YYYY-MM-DD.csv` with BOM) + window.print() trigger; **Settings tab**: 4 alert threshold number inputs + default report dropdown + save + reset-defaults + saved confirmation timestamp. TH/EN copy 56 strings. Wired into `WorkspaceAppPanel` after Procurement; `supabase/migrations/0009_project_control_settings.sql` (project_control_settings table — single row per workspace as primary key + JSONB alert_thresholds with default jsonb_build_object + CHECK on default_report_type + workspace-only RLS + updated_at trigger). Print CSS rules added to `styles.css` `@media print` — hide nav/topbar/summary chrome via `.no-print/.cf-topbar/.module-hero/.summary-grid`, strip borders+padding on `.project-control-print` and add table border-bottom + 11px font for clean PDF/print output. Verification: `npm test -- --run` passed 429 tests (44 new: 30 projectControl + 14 csvExport), `npx tsc --noEmit` clean, `npm run build` clean (workspace 546 KB gzip 135 KB), `curl /project-control?tab=dashboard,reports,settings` returns 200. **Strategic context**: per `docs/POJJAMAN_GAP_ROADMAP.md` Section 4 + `docs/ERP_ADAPTATION_PLAN.md` Section 7, this completes the "first credible ERP loop" — create project → ขอซื้อ → เทียบราคา → เลือก supplier → บันทึกจ่ายจริง → เห็นกำไร/ขาดทุนโครงการ. Next sprints per roadmap: Sprint 7 (Approval + Audit Core) → Sprint 8 (Evidence Asset Layer) → Sprint 9 (Inventory Lite).

- 2026-05-24: **Builk Parity Sprint 5 — Cashflow project extension + rollup engine + recurring templates shipped.** Extended `CashflowEntry` with 8 new optional fields (costCodeId/supplierId/prId/rfqId/poDocumentId/quantityActual/unitActual/recurringTemplateId) + `sourceType`/`sourceDocumentId` enum (added during Codex refactor — auto-derives from PR/RFQ/PO/invoice/recurring links so filters work without per-flag heuristics). Backward compat: old `CashflowEntry` records without these fields load with empty defaults via normalize. Created `src/cashflow.rollup.ts` (pure functions) — `computeProjectRollup(projectId, entries)` returns `{actualCost, actualRevenue, costCodeRollups[], lastEntryAt, confirmedEntryCount}` summing only `status='confirmed'` entries; `syncProjectFromCashflow(projectId, cashflowState, projectState)` returns updated ProjectListState with project totals recomputed + change-detection (no-op when totals unchanged so React doesn't churn); `syncProjectsFromCashflow` recomputes every project; `filterCashflowEntries(state, opts)` with project/costCode/supplier/direction/status/sourceType/search dimensions (the rich filter replaces my hand-rolled source-detection branch in cashflow.ts since `sourceType` enum makes it cleaner); `buildSupplierPriceHistoryEntryFromCashflow` returns SupplierPriceHistoryEntry with id `cashflow-price-{entryId}` (or null when supplierId/costCodeId missing, direction!=expense, status!=confirmed); `syncSupplierPriceHistoryFromCashflow` upserts (replaces existing entry from same cashflow id so re-sync doesn't duplicate); `removeCashflowPriceHistory` cleanup on entry delete. Created `src/cashflow.recurring.ts` — `RecurringTemplate` type + `RECURRING_STORAGE_KEY = "cashflow.recurring.v1"` + storage adapter helpers + `generateEntriesFromTemplate(template, today)` iterating monthly/weekly/quarterly/yearly periods since `lastGeneratedDate` (or `startDate` if first run) up to today, respecting `endDate` + `active`, sets `recurringTemplateId` on generated entries as `status='draft'` for user review, idempotent on re-call (advances `lastGeneratedDate` so second call yields zero) + `generateAllRecurring` aggregator. **Tests**: `src/cashflow.rollup.test.ts` 7 tests by Codex (sums by status/direction/project, excludes draft+void, groups by cost code, sync no-op on unchanged totals, supplier price history dedup); `src/cashflow.recurring.test.ts` 23 tests by me (storage round-trip, normalize defaults, upsert/remove, generate per frequency 5 cases incl. resume-from-last, skip inactive, respect endDate, sets template id, default draft status, idempotent re-run, projectId/costCodeId/supplierId propagation, generateAllRecurring aggregator). UI: `CashflowPanel` already extended by Codex with Project / Cost Code / Supplier dropdowns + actual qty/unit fields + project/cost-code/supplier filter rows + linked-meta chips; `persist` triggers both `syncProjectsFromCashflow` and `syncSupplierPriceHistoryFromCashflow` on every save/remove. Wired Project Detail "บันทึกต้นทุน" tab — new `ProjectCashflowTab` component shows scoped table of all cashflow entries for the open project (filtered via `filterCashflowEntries({projectId})`), category column + cost-code column + amount with income/expense color + status, per-cost-code rollup summary section below, link-out to `/cashflow` for adding new entries (TH/EN). `supabase/migrations/0008_cashflow_extension.sql` ALTER cashflow_entries with 10 new columns (8 PRD + sourceType + sourceDocumentId) + 6 indexes + DO-block source_type CHECK constraint (idempotent for re-run) + CREATE `recurring_templates` table 14 columns + 3 indexes + 4 RLS policies via `is_workspace_member` + updated_at trigger. Verification: `npm test -- --run` passed 381 tests (30 new), `npx tsc --noEmit` clean, `npm run build` clean (workspace 519 KB gzip 128 KB), `curl /cashflow` + `/projects` + `/procurement` returns 200. Manual interactive smoke (add cashflow entry with project+cost code+supplier → confirm → check Project Detail → cost tab now shows entry + rollup; supplier detail recent prices auto-populated) requires click-through; each rollup computation + recurring generator path has unit test coverage.

- 2026-05-24: **Builk Parity Sprint 4 — RFQ comparison + award + auto price history shipped.** Extended `src/procurement.ts` with the RFQ side of the workflow: state machine `transitionRFQStatus` (6 states × 5 actions — send/record_response/complete/award/cancel) with required-context guards (award needs awardedSupplierId + awardReason); `recomputeRFQStatus` derives status from response coverage (sent→partial_response when first response in; →responses_complete when all invited responded; terminal awarded/cancelled unchanged); `recordResponse` validates the responder is invited + rejects on awarded/cancelled RFQ + auto-recomputes status + dedup by supplierId; `removeResponse` reverts complete→partial; `buildComparisonMatrix(rfq, prItems)` returns rows = items × cells with `isBest` flag for cheapest available quote per row + per-supplier totals + `bestTotalSupplierId` (prefers fully-quoted suppliers, falls back to partial when none complete); AwardReason union + `awardReasonCopy`; `awardRFQ(rfq, pr, supplierId, reason)` is the side-effect-free award orchestrator — validates (invited / response present / PR matches RFQ.prId / reason given / not already awarded) then returns `{rfq awarded, pr advanced to awarded with linkedRfqId stamped, priceHistoryAppendages[]}` for the caller to apply; filterRFQs/summarizeRFQs/removeRFQ. **44 new tests** in `src/procurement.rfq.test.ts` covering storage + numbering, draftRFQFromPR throw-when-non-approved, state machine all 5 actions × happy + illegal + missing context, recordResponse 5 cases (reject non-invited, reject on awarded, partial→complete progression, dedup, removeResponse reverts), recomputeRFQStatus leaves terminals alone, buildComparisonMatrix highlights cheapest + computes amount from qty when missing + marks unavailable cells + bestTotal logic + partial-fallback + zero rows, awardRFQ happy path stamps PR + builds priceHistory entries with correct cost code + 6 error cases (not invited / no response / PR mismatch / empty reason / already awarded / unavailable items excluded), filter+summarize. UI: ProcurementPanel rewired — **PR "Send RFQ" action now creates draft RFQ from PR via `draftRFQFromPR` + auto-numbers via `nextRfqNumber` + opens RFQ detail** instead of bare status flip; replaced RfqStub with **RfqListTab** (7-status filter chips + 6-col table with PR No back-link + project + status badge + invited count + responses ratio green when complete) and **RfqDetail** with 3-section layout: header (back + RFQ No + status badge + source PR back-link + awarded supplier display + send/cancel/delete), **Invite suppliers** grid of multi-select checkboxes with rating display, **Record responses** inline form per invited supplier (per-item unit-price + no-bid toggle + payment terms / delivery / validUntil / notes; collapsed summary card with edit/remove when saved), **Compare quotes** item×supplier matrix table with cheapest cell highlighted green + per-supplier totals row with ★ BEST TOTAL + Award button per responding supplier (reason picked via numbered `window.prompt` list); Award triggers `handleRfqAward` → `awardRFQ` → updates RFQ + PR + appends each priceHistoryAppendages entry to SupplierState via `addPriceHistoryEntry` (visible immediately in Supplier detail → recent prices). URL-driven `?rfqId=` for detail; popstate keeps both `?id=` and `?rfqId=` in sync. TH/EN copy 24 new strings. `supabase/migrations/0007_rfqs.sql` 3 tables (rfqs + rfq_responses with unique constraint per supplier per RFQ + rfq_item_quotes) + 9 indexes + 12 RLS policies nested via rfqs.workspace_id + updated_at trigger. Verification: `npm test -- --run` passed 351 tests (44 new), `npm run build` clean (workspace 507 KB gzip 127 KB), `curl /procurement?tab={pr-list,rfq-list,archive}` all return 200. Manual interactive smoke (PR approved → Send RFQ → invite 3 suppliers → record 2 responses with prices → see best-price highlight → award supplier → verify PR status awarded + supplier price history populated) requires click-through; each transition + matrix computation has unit test coverage.

- 2026-05-24: **Builk Parity Sprint 3 — Procurement (Purchase Request) workflow shipped.** Created `src/procurement.ts` (combined PR + RFQ schema in one module since they share auto-numbering and Project/CostCode/Supplier linkage; two separate storage keys `procurement.pr.v1` + `procurement.rfq.v1` so PR mutations don't trigger RFQ writes; PR domain: PRLineItem with amount = qty × estUnitPrice auto-compute; PurchaseRequest with `totalAmount` derived from items; nextPrNumber `PR-{YYYY}-NNN` with year-reset + 3-digit pad + ignore-malformed; nextRfqNumber same shape; **pure-function state machine** `transitionPRStatus(current, action, ctx)` with 10 states (draft/submitted/approved/rejected/rfq_sent/awarded/ordered/received/closed/cancelled) × 10 actions; required-context guards — approve needs approverId, reject needs reason, award needs awardedSupplierId; `canTransition` / `legalActionsFor` boolean+list helpers; `applyPRAction` stamps approvedBy/rejectedReason/linkedRfqId/linkedPoDocumentId and clears rejection on edit; validatePR; filterPRs/summarizePRs; draftRFQFromPR scaffold seeding invitedSupplierIds from preferred suppliers ready for Sprint 4); `src/procurement.pr.test.ts` 52 tests (storage / amount-compute / clamp-negative / nextPrNumber year-reset+pad+ignore-junk / validation 5 cases / state machine — all 10 actions × happy path × illegal blocked × context-required throws / applyPRAction field stamps / upsert/remove / filter combined / summarize byStatus+pendingApproval+inProgress+totalAmount / createPR defaults + draftRFQFromPR throws when PR not approved / preferred-supplier preflight); added `procurement` to `src/apps.ts` (route `/procurement`, procurement sidebar group as third member after costCodes+suppliers, taxonomy construction/freemium/write/workspace/optional AI); updated shell files `workspace/shell/workspaceGroups.ts` (ClipboardList icon + appIds extended) + `workspaceRouting.ts` (subnav pr-list/rfq-list/archive) + `workspaceCopy.ts` (TH/EN); built `src/workspace/apps/procurement/ProcurementPanel.tsx` — **PR list tab**: 4 summary tiles + 8-status filter chips + project filter dropdown + free-text search + 6-col table (PR No / project name / status badge / item count / amount / needed-by) clickable rows + create button; **PR detail/form tab**: header with PR No + status badge + dynamically built action button toolbar via `legalActionsFor()` (Submit→Approve/Reject→Send RFQ/Award/Order→Receive→Close + Cancel + Edit back to draft); project dropdown + dates + notes + **inline line-items editor**: Cost Code dropdown populated from `loadCostCodes()` → on select auto-fills description/unit/unitPrice from `defaultUnit`+`defaultUnitPrice`; preferred supplier dropdown from `loadSuppliers()`; amount auto-compute per row + grand total footer; add/remove row; readonly mode for non-draft/rejected with "Edit (back to draft)" path; rejected reason banner; window.prompt-based action context capture (reject reason / approver id / awarded supplier id / linked PO doc id); delete with confirm; **Archive tab**: closed+cancelled PRs; **RFQ tab**: placeholder pending Sprint 4. URL-driven `?id=` for detail. Wired into `WorkspaceAppPanel` after Suppliers; `supabase/migrations/0006_purchase_requests.sql` (purchase_requests + pr_line_items tables + 7 indexes + workspace-only RLS for both + updated_at trigger; cross-link notes for cost_code_id/preferred_supplier_id/project_id/linked_po_document_id). Verification: `npm test -- --run` passed 307 tests (52 new), `npm run build` clean (workspace 480 KB gzip 121 KB), `curl /procurement` + `/suppliers` + `/cost-codes` + `/projects` returns 200. Manual interactive smoke (create PR → cost-code dropdown fills defaults → save → submit → approve → send_rfq → award → order → receive → close) requires click-through; state machine + each transition path has unit test coverage.

- 2026-05-24: Start App.tsx module split Phase 3 - created `src/workspace/shell/workspaceRouting.ts`, `workspaceCopy.ts`, `workspaceGroups.ts`, and `workspaceLanguage.ts` as shared shell modules for app route parsing/building, subnav definitions, version-selection storage, language persistence, shell copy, app groups, and icon lookup. Updated `src/App.tsx` to import these helpers while keeping shell UI components in place for a low-risk split. Added `workspaceRouting.test.ts` and `workspaceLanguage.test.ts`; included shell helpers in coverage config. Verification: current full suite `npm test -- --run` passed 381 tests, `npm run build` passed with the existing Vite chunk-size warning.

- 2026-05-24: **Builk Parity Sprint 2 — Suppliers directory + price history shipped.** Created `src/suppliers.ts` (combined Supplier + SupplierPriceHistoryEntry into one SupplierState + storage via adapter + validateSupplier with Thai tax_id 13-digit check + email regex + rating 0-5 clamp + detectSupplierType heuristic บจก./บมจ./บริษัท/หจก./ห้างหุ้นส่วน/ร้าน/รับเหมา/ก่อสร้าง + Co./Ltd English suffix + formatThaiTaxId formatter `0-1057-12345-67-8` + stripTaxIdFormat + upsertSupplier + deactivateSupplier soft + removeSupplier cascade-deletes price history + addPriceHistoryEntry computes totalAmount when missing + removePriceHistoryEntry + searchSuppliers with digits-only guard so empty-string tax-id branch does not match all + filterSuppliers (type/city/activeOnly) + sortSuppliers (name/spend/lastOrder/rating, name uses th-TH collation) + computeSupplierSummary 12-month window with topCostCodeIds top-3 by spend + recentPricesForSupplier with driftPct vs previous same-code entry + summarizeSupplierDirectory + createSeedSuppliers 5 Thai suppliers SCC/TPI/Insee/NorthMat/ThaiSteel + ensureSeedSuppliers idempotent + parseCsv with quoted-field/CRLF + parseSupplierRow with field aliases (short/shortname, tax_id/taxid/vat, line_id/line, postal_code/postal/zip, payment/payment_terms, note/notes) + auto-detect type from name when column missing + tags split on `,;|` + parseSuppliersCsv + applySupplierCsvImport with dedupe-by-tax_id-else-name); `src/suppliers.test.ts` 54 tests; added `suppliers` to `src/apps.ts` manifest (route `/suppliers`, procurement sidebar group, taxonomy construction/freemium/write/workspace/optional AI) + Users icon + subnav directory|price-history|import|analytics + TH/EN copy in `workspace/shell/workspaceCopy.ts` + `workspace/shell/workspaceGroups.ts` + `workspace/shell/workspaceRouting.ts` (which Codex extracted in parallel); built `src/workspace/apps/suppliers/SuppliersPanel.tsx` — Directory tab split-view: left list shows rating stars + total spend + order count with type filter + 4-key sort + empty-state offers seed action, right detail shows header KPIs + formatted tax_id + tags pills + notes + edit/deactivate + inline PriceHistoryForm with cost code autocomplete from `loadCostCodes()` + recent prices table with ↑↓ drift % indicator + remove price entry; Price History tab (cross-supplier table sorted by date desc); Import tab (paste/file CSV + preview 30 rows + skip vs update toggle + result toast); Analytics stub Sprint 6. TH/EN copy. Wired into `WorkspaceAppPanel` after CostCodes; `supabase/migrations/0005_suppliers.sql` (suppliers + supplier_price_history tables + 8 indexes + workspace-only RLS for both + updated_at trigger). Verification: `npm test -- --run` passed 247 tests (54 new), `npm run build` clean (workspace 453 KB gzip 114 KB), `curl /suppliers` + `/cost-codes` + `/projects` returns 200. Manual interactive smoke (add price history with cost code autocomplete, drift % display, CSV import) requires click-through.

- 2026-05-24: **Builk Parity Sprint 1 — Cost Codes (CBS) catalog + import/export shipped.** Created `src/costCodes.ts` (CostCode + CostCodeState + storage adapter + validateCostCode (unique-per-workspace, parent-must-exist incl. system seeds, custom-unit required when defaultUnit='custom', parent ≠ self) + upsertCostCode + deactivateCostCode soft-delete + removeCostCode + searchCostCodes case-insensitive + filterCostCodesByCategory + groupCostCodesByParent + summarizeCostCodes + ensureSeedCostCodes idempotent + parseCsv with quoted-field/CRLF support + mapUnitLabelToCostCodeUnit Thai (ตร.ม./ลบ.ม./เมตร/ชิ้น/ชุด/ต้น/kg/ตัน) + parseCostCodeCsv with header aliases + applyCsvImport (skip_duplicates vs update_existing) + exportCostCodesToCsv with proper escaping); `src/costCodes.seed.ts` 100 Thai cost codes (site 10 / structure 15 / architecture 20 / mep 15 / finishing 20 / external 10 / indirect 10 incl. roots) priced ตามราคากลาง 2025-2026; `src/costCodes.test.ts` 55 tests (storage / normalize / validation 6 cases / upsert/remove / search/filter/group / seed integrity 6 cases / ensure idempotent / parseCsv quoted+CRLF / mapUnitLabel / parseCostCodeCsv with thousand-separator + alias headers + missing-fields / applyCsvImport 4 modes / exportCostCodesToCsv round-trip + escaping / summarize); added `costCodes` to `src/apps.ts` (route `/cost-codes`, taxonomy construction/freemium/write/workspace/optional AI); new sidebar group `procurement` "Cost Control" in `App.tsx` (TH/EN group copy + Database icon + subnav catalog|import|export|usage); built `src/workspace/apps/cost-codes/CostCodesPanel.tsx` — Catalog tab (4 summary tiles + search box + tree view per category with collapse/expand + add/edit form with parent dropdown + soft-delete with confirm + SEED/INACTIVE badges + Thai/English unit shorts), Import tab (paste CSV textarea + file picker + preview table 30 rows + mode toggle + apply with result toast), Export tab (active-only toggle + dated filename download), Usage stub. Wired into `WorkspaceAppPanel` after Projects; `supabase/migrations/0004_cost_codes.sql` (cost_codes table + 4 indexes + RLS read system-seed shared (workspace_id IS NULL) + write workspace-only + updated_at trigger + 7 root seed rows). Verification: `npm test -- --run` passed 193 tests (55 new), `npm run build` clean (workspace 411 KB gzip 105 KB), `curl /cost-codes` + `/projects` returns 200. Manual interactive smoke (CSV import preview/apply, soft-delete, tree expand) requires click-through.

- 2026-05-24: Continue App.tsx module split Phase 2 - created `src/workspace/apps/boq-data/boqDataService.ts` for BOQ catalog storage through `StorageAdapter`, BOQ row normalization, search text, merge/override rules, price status/filter constants, and CSV parsing; added `boqDataService.test.ts`; updated `App.tsx` to import the service while keeping `BoqDataPanel` UI in place; fixed CSV column matching to prefer exact header matches before partial matches so `note` does not collide with `keynote`; fixed build typing for `projects` app copy and seed project typing. Verification: `npm test -- --run` passed 193 tests, `npm run build` passed, Playwright smoke `/boq-data?tab=database&version=0.1` searched `A1000` and `/boq-data?tab=task-linkage&version=0.1` rendered layout. In-app Browser local URL smoke was blocked by client, so Playwright local fallback was used.

- 2026-05-24: **Builk Parity Sprint 0 — Project entity + List/Detail app shipped.** Created `src/projects.ts` (16-field Project model + ProjectComputed at read time + storage via adapter + suggestNextProjectCode `j-{YY}{seq}` pattern + computeProject (daysRemaining/marginPct/isOverdue/isOverBudget) + applyAutoStatus (normal → delayed when overdue or actual > 1.05 × planned) + summarizeProjectList + detectCustomerType (Thai gov/corp heuristic) + createSeedProjects + ensureSeedProjects); `src/projects.test.ts` 40 tests; added `projects` app to `src/apps.ts` manifest (route `/projects`, taxonomy: construction/freemium/write/workspace/optional AI) + sidebar group Project Work + subnav list|archive|analytics + icon Building2 in `App.tsx`; built `src/workspace/apps/projects/ProjectsPanel.tsx` + `projectsPanelCopy.ts` (TH/EN) — list view with recent-active carousel + 5-state filter chips + search + create-form drawer + 9-col sortable table; detail view with header KPIs + 7-tab nav (overview live; PR/RFQ/PO/cost/invoice/reports placeholder) + alert banners (overdue + over-budget) + cost-breakdown placeholder + AI insights dark card + edit/delete; auto-detect customerType from clientName Thai prefix; URL-driven detail switching via `?id=`; wired into `WorkspaceAppPanel` after Cashflow; `supabase/migrations/0003_projects.sql` (projects table 16 fields + 4 indexes + RLS via `is_workspace_member` + updated_at trigger). Verification: `npm test -- --run` passed 138 tests (40 new), `npm run build` clean (367 KB workspace gzip 93 KB), `npm run preview` + `curl /projects` returns 200. Browser interactive smoke (create form, detail tabs, edit/delete) requires manual click-through.

- 2026-05-24: Continue App.tsx module split Phase 2 - created `src/workspace/apps/defects/defectService.ts` for defect labels, legacy `defect-tracker.records.v1` cleanup through `StorageAdapter`, file-size/date formatters, and image upload/resize helper; added `defectService.test.ts`; updated `App.tsx` to import the service while keeping `DefectTrackerPanel` UI in place. Verification: `npm test -- --run` passed 92 tests, `npm run build` passed, browser smoke `/defect?tab=overview&version=0.1` opened dashboard, added a defect, and returned to `/hub`.

- 2026-05-24: Continue App.tsx module split Phase 2 - created `src/workspace/apps/employees/employeeService.ts` for Employee project options, default seed employees, record normalization, site-team derivation, legacy `employees.workspace.v1` cleanup through `StorageAdapter`, payroll totals, allocation counts, and app stats; added `employeeService.test.ts`; updated `App.tsx` to import the service while keeping `EmployeePanel` UI in place. Verification: `npm test -- --run` passed 88 tests, `npm run build` passed, browser smoke `/employees?tab=overview&version=0.1` opened dashboard, added an employee, opened payroll tab, and returned to `/hub`.

- 2026-05-24: ปิด PRD pipeline ครบ Sprint 0-7 + Migration Guide สำหรับ alpha test — `docs/CASHFLOW_PROJECT_EXTENSION_PRD.md` (Sprint 5: extend CashflowEntry 8 new fields + RecurringTemplate + computeProjectRollup pure function + syncProjectFromCashflow + SupplierPriceHistory auto-append + SQL migration 0008 alter columns); `docs/MIGRATION_FROM_BUILK.md` customer-facing Thai-primary 10 sections (TL;DR comparison table, 15-min quick start CSV import, Terminology mapping, workflow mapping 5 flows, advantages/honest disadvantages, 30-day Hybrid mode plan, pricing comparison, FAQ, public Sprint roadmap, contact channels — alpha tester recruitment piece). Build clean หลัง Codex finish P0-018 split.
- 2026-05-24: Start App.tsx module split Phase 2 - created `src/workspace/apps/social-feed/socialFeedService.ts` for Social Feed types, seed data, normalization, storage adapter load/save, post/comment creation, search text, stats, time formatting, and image resize helpers; added `socialFeedService.test.ts`; updated `App.tsx` to import the service while keeping `SocialFeedPanel` UI in place. Verification: `npm test -- --run` passed 83 tests, `npm run build` passed, browser smoke `/feed?tab=feed&version=0.1` created a post, opened profile tab, and returned to `/hub`.
- 2026-05-24: เขียน 2 sub-PRDs เพิ่มปิด pipeline ถึง Sprint 6 — `docs/PROCUREMENT_PRD.md` (Sprint 3+4 รวม 12 sections: PR + RFQ shared module, auto-numbering PR-{YYYY}-NNN + RFQ-{YYYY}-NNN, 10-state PR machine + 6-state RFQ machine, RFQ matrix comparison + award flow + auto-create PO in BuildDocs + auto-append SupplierPriceHistory, validation requirements + Section 8 tests outline 7 test groups, SQL migrations `0006_purchase_requests.sql` + `0007_rfqs.sql` รวม 5 tables + RLS); `docs/PROJECT_CONTROL_PRD.md` (Sprint 6 รวม 12 sections: pure aggregation no new entities, computeProjectSnapshot logic, 7 alert types + customizable thresholds, 5 standard reports (project P&L / cashflow forecast / cost variance / supplier spend / PR aging), CSV export with UTF-8 BOM for Thai, print CSS, optional project_control_settings table only). Codex Sprint backlog ครบ Sprint 0-6 พร้อม implement.
- 2026-05-24: เขียน 2 sub-PRDs ให้ Codex มี backlog Sprint 1+2 พร้อม implement — `docs/COST_CODES_PRD.md` (12 sections: storage contract, CostCode type + validation, 100 Thai seed catalog 7 categories, UI flow tree+CSV import, acceptance criteria, tests outline, SQL migration draft `0004_cost_codes.sql` + RLS with system seed shared read), `docs/SUPPLIERS_PRD.md` (12 sections: Supplier + PriceHistory types, Thai tax_id formatter, type auto-detect heuristics, split-view list+detail UI, CSV import, SQL migration draft `0005_suppliers.sql` 2 tables + RLS, AI auto-rate hint). อัปเดต `docs/PRD.md` App Map เพิ่ม 4 apps (projects/costCodes/suppliers/procurement/projectControl) พร้อม sprint status + PRD link + mockup link.
- 2026-05-24: Project Detail mockup + PROJECT_PRD.md sub-PRD พร้อมส่ง Codex implement — เพิ่ม `ProjectDetailMockup` ใน gallery: project header card (status badge + 5 KPIs + 3 action buttons), 7-tab navigation (ภาพรวม/PR/RFQ/PO/บันทึกต้นทุน/ใบแจ้งหนี้/รายงาน) พร้อม count badges, overview tab มี 2 alert banners (overdue + over-budget per cost code), cost breakdown bars 6 codes, recent activity feed + AI insights dark card (Phase 2 hints: margin warning, benchmark comparison, partial billing suggestion); stub panels สำหรับ tab อื่นๆ ที่ลิงก์ไป standalone mockup. เขียน `docs/PROJECT_PRD.md` 12 sections (purpose/non-goals/storage/data model + code convention + state machine + computed fields/UI flow/acceptance criteria/tests outline/SQL migration draft 0003_projects.sql/codex implementation notes) — ready-to-implement Sprint 0 spec. 77 tests ผ่าน build clean.
- 2026-05-24: Builk `/costcontrol/project/search` analysis applied — เพิ่ม `ProjectListMockup` ใน MockupGallery (carousel 4 active project cards พร้อม 3 KPIs + 5-state filter chips ปกติ/ชะลอ/สิ้นสุด/ยกเลิก + 9-col sortable table + company switcher + "ไม่มีลูกค้า"/"ไม่มีงบประมาณ" empty states + negative daysRemaining สีแดง + monetization counter "เครดิตคงเหลือ"); แทรก **Sprint 0** "Project Entity + List page" ใน `docs/BUILK_PARITY_PLAN.md` ก่อน Cost Codes (เพราะ Project = entry point ของ Builk ไม่ใช่ Cost Codes); ขยาย Project data model จาก 7 fields → 16 fields (4 financial fields + status enum + dates + computed marginPct/daysRemaining + customerType + project code convention); ดัน Sprint เดิม 1-8 → 1-12 พร้อม AI Phase 2 plan. 77 tests ผ่าน build clean.
- 2026-05-24: `/mockup` gallery (clickable preview ของ Builk-parity modules) — สร้าง `src/MockupGallery.tsx` รวม 5 mockups: Cost Codes (CBS tree + search + Thai seed ~20 codes), Suppliers (5 ราย + price history + rating + spend tracker), Purchase Request (5 PRs list + status badges + filter chips), RFQ Comparison (4 items × 3 suppliers matrix table + best-price highlight + Award button), Project Control (6 cost codes × budget/committed/actual bars + variance alert + 5 standard reports). + เพิ่ม `/mockup` ใน PUBLIC_ROUTES, route handler ใน PublicSite.tsx. ใช้ Thai sample data + workspace CSS classes เดิมเพื่อ visual consistency. ยังไม่มี persistence — ใช้เป็น preview สำหรับ owner decision + alpha tester feedback + Codex implementation starting point. AI superpower placeholders ใน each mockup ระบุว่า Phase 2 จะเพิ่มอะไร. 77 tests ผ่าน, build clean.
- 2026-05-24: `docs/BUILK_PARITY_PLAN.md` — decision document หลังเห็น Builk landscape (30K user, procurement-style workflow); Section 0 framework + Section 1 Builk module inventory 13 items + Section 2 Buildbybim asset map (gap: suppliers, PR/RFQ, project-scoped cost recording, project dashboard, reports) + 3 strategy variants (A full parity → AI overlay / B selective + AI native / C AI-first) พร้อม pros/cons + 8-16 weeks timeline; Section 4 module sketches (CostCode TS shape, Supplier, PR, RFQ comparison view, Project Cost Dashboard ascii); Section 5 new apps to add (costCodes, suppliers, procurement, projectControl) + sidebar group `procurement`; Section 6 SQL migration draft `0003_cost_control.sql`; Section 7 migration story from Builk (CSV import + terminology mapping + free tier wider); Section 8 differentiation matrix vs Builk; Section 11 7 open questions owner ต้องตอบก่อนเริ่ม; Section 12 Sprint 1 concrete first step (Cost Codes CBS spine).
- 2026-05-23: ปิด P0-003 test strategy — เพิ่ม `src/sheets.test.ts` ครอบคลุม Google Sheet CSV URL, quoted CSV, thousand separators, invalid qty/price behavior, default unit, missing required columns; เพิ่ม `src/workspaceStorage.test.ts` ครอบคลุม `calculateTotal` VAT/withholding/milestone invoice และ workspace import/save normalizers (documents sort, active id fallback, clients/projects derive, employee/site team/defect/photo normalization, invalid backup rejection); อัปเดต `vite.config.ts` coverage include ให้รวม `storage.ts` + `sheets.ts`; `npm test -- --run` ผ่าน 77 tests และ `npm run build` ผ่าน.
- 2026-05-23: ปิด P0-004 conflict-risk planning — สำรวจ `src/App.tsx` ประมาณ 10,895 บรรทัดและสร้าง `docs/APP_TSX_MODULE_SPLIT_PLAN.md` ระบุ target folder shape, split order 4 phases, guardrails, recommended first move (`CashflowPanel`), และ verification checklist เพื่อให้ agent หลายตัวแยกงานโดยไม่ย้ายโค้ดชนกัน.
- 2026-05-23: เริ่ม App.tsx split phase 1 — ย้าย `PageHeader` และ `SummaryTile` ไป `src/workspace/shared/`, ย้าย `CashflowPanel` ไป `src/workspace/apps/cashflow/CashflowPanel.tsx`, ลด `src/App.tsx` จากประมาณ 10,895 เหลือ 10,435 บรรทัด; `npm test -- --run` ผ่าน 77 tests, `npm run build` ผ่าน, browser smoke `/cashflow?tab=overview&version=0.1` พร้อม warning + Back to Hub ผ่าน.
- 2026-05-23: ต่อ App.tsx split phase 1 — ย้าย `AgentChatPanel` ไป `src/workspace/apps/agent-chat/AgentChatPanel.tsx`, ให้ module มี tab resolver/file helper/copy ของตัวเอง, ลด `src/App.tsx` เหลือประมาณ 10,175 บรรทัด; `npm test -- --run` ผ่าน 77 tests, `npm run build` ผ่าน, browser smoke `/agent-chat?tab=chat&version=0.1` ส่งข้อความ + channel tab + Back to Hub ผ่าน.
- 2026-05-23: ต่อ App.tsx split phase 1 — ย้าย `DesignStudioPanel` ไป `src/workspace/apps/design-studio/DesignStudioPanel.tsx`, แยก workflow/prompt copy และ Design UI ออกจาก shell, ลด `src/App.tsx` เหลือประมาณ 9,889 บรรทัด; `npm test -- --run` ผ่าน 77 tests, `npm run build` ผ่าน, browser smoke `/design?tab=envision&version=0.1` เปลี่ยน workflow เป็น ReDesign + Back to Hub ผ่าน.
- 2026-05-23: ปิด App.tsx split phase 1 leaf panels — ย้าย `LibraryPanel` ไป `src/workspace/apps/library/LibraryPanel.tsx`, แยก image gallery/prompt/document drawer ออกจาก shell, ลด `src/App.tsx` เหลือประมาณ 9,660 บรรทัด; `npm test -- --run` ผ่าน 77 tests, `npm run build` ผ่าน, browser smoke `/library?tab=images&version=0.1` next image + Prompt tab + Back to Hub ผ่าน.
- 2026-05-23: Master PRD sync ครบรอบ — เพิ่ม Section 0 "Current Implementation Snapshot" ครบ (0.1 platform skeleton + 0.2 implemented apps table + 0.3 public routes + 0.4 architecture layers + 0.5 data modules + 0.6 quality/tests + 0.7 files reference + 0.8 next priorities + 0.9 how to run); update Section 6 Architecture สะท้อน implemented adapter chain + Supabase Phase A→F; Section 9 Cashflow + Membership mark v0.1 ready ครบ; Section 12 Release Plan tick v0.1-v0.5 status; Section 13 Open Questions แยก Resolved/Still-open/For-Codex 6 หัวข้อใหม่. + สร้าง `docs/IMPLEMENTED_ERD.md` แยกจาก conceptual PLATFORM_ERD.md แสดง Supabase tables ที่ live + local TS types + mapping table + access evaluation flowchart + entity counts. พร้อมส่งต่อให้ Codex continue dev.
- 2026-05-23: Supabase Phase F — Auth UI live — สร้าง `src/auth.ts` (magic link sign-in + anonymous + signOut + onAuthChange subscribe + `getMyWorkspaces` + `ensureWorkspace` auto-create); refactor `/account` page ใน PublicSite จาก placeholder เป็น 3 states (Supabase not configured / loading / sign-in form 3-card layout / signed-in card พร้อม workspace list + sign out + create workspace button); ใน `/admin?tab=overview` Cloud Sync card auto-pick workspace จาก auth session — ถ้ามี membership แล้วโผล่เป็น select dropdown, ถ้าไม่มีมีปุ่ม "Create my workspace" สร้างให้อัตโนมัติ; SUPABASE_SETUP.md เพิ่ม Section URL configuration + Auth UI flow + mark Phase F done. funnel ครบ: landing → /apps → workspace → access gate → /support-plans → /account sign-in → cloud sync ทำงาน.
- 2026-05-23: Supabase Phase C — SupabaseAdapter + Cloud Sync UI — สร้าง `supabase/migrations/0002_kv_store.sql` (kv_store table generic JSONB + RLS via `is_workspace_member()` + updated_at trigger); สร้าง `src/supabaseAdapter.ts` (SupabaseAdapter class implements StorageAdapter, local cache + 800ms debounced background flush + `pullFromCloud`/`pushToCloud`/`flushPending`/`getStatus`; `LOCAL_ONLY_PREFIXES` กัน `buildbybim.auth.*` + `build-by-bim.workspace-language` ไม่ให้ leak ขึ้น cloud); `activateSupabaseSync(workspaceId)` / `deactivateSupabaseSync` swap default adapter ผ่าน `setStorageAdapter`; เพิ่ม Cloud Sync card ใน `/admin?tab=overview` แสดง status + ปุ่ม Activate/Push/Pull/Disconnect + workspace UUID input + TH/EN messaging. 63 tests ผ่าน (11 ใหม่: SupabaseAdapter cache path + isLocalOnlyKey). อัปเดต SUPABASE_SETUP.md mark Phase C done + step 6 ขยายเป็นคู่มือใช้ Cloud Sync card.
- 2026-05-23: Supabase Phase A connection scaffold — ติดตั้ง `@supabase/supabase-js`, `.env.example` + `.gitignore` แยก env, `src/vite-env.d.ts` types สำหรับ `VITE_SUPABASE_*`, สร้าง `src/supabaseClient.ts` (null-safe singleton ที่ return null ถ้าไม่มี env, มี `isSupabaseConfigured`/`isSupabaseSyncEnabled`/`getSupabaseClient`/`supabaseConnectionStatus`), เขียน `supabase/migrations/0001_initial_platform.sql` ตาม PLATFORM_ERD Section 4 (7 ตาราง + RLS policies ใช้ `is_workspace_member(uuid)` helper + updated_at triggers), เขียน `docs/SUPABASE_SETUP.md` step-by-step (get keys → paste .env → run SQL → create workspace → enable auth → deploy env vars → verify); build ผ่าน + 52 tests ยังผ่าน — app ยังทำงาน local-first ปกติเพราะ Supabase client ทำงานแบบ opt-in.
- 2026-05-23: Test foundation + social asset — ติดตั้ง Vitest + jsdom + @vitest/coverage-v8 + scripts `test`/`test:watch`/`test:coverage`; เขียน 3 test files (52 tests ผ่านทั้งหมด): `storageAdapter.test.ts` (Local + Memory adapters + readJson/writeJson roundtrip + swappable singleton), `membership.test.ts` (PRD Section 5 evaluation order ทั้ง 6 steps + seedPlans + audit cap + upsert), `cashflow.test.ts` (summarize + month boundary + draft exclusion + remove); สร้าง `public/og-cover.svg` 1200×630 monochrome brand + bundled ใน `dist/og-cover.svg` พร้อมแชร์ Facebook/X — อัปเดต OG meta จาก .png เป็น .svg; P0-003 → in_progress.
- 2026-05-23: Code-split + adapter expansion ปิด architecture skeleton — main.tsx ใช้ `React.lazy` แยก workspace App ออกจาก initial bundle (landing visitor ได้ 421 KB / gzip 113 KB แทน 738 KB / gzip 193 KB เดิม; workspace 317 KB / gzip 80 KB โหลดเฉพาะตอนเข้า /hub หรือ workspace route); refactor `membership.ts` + `boqTaskLinkage.ts` + `stylePreviewI18n.ts` ใช้ `defaultStorageAdapter` + `readJson`/`writeJson` แทน `window.localStorage` ตรง ครบทุก data module (-68 บรรทัด guard/try-catch ที่ delegated ให้ adapter) — A-002 (Plan backend sync) → done, พร้อมเพิ่ม Supabase/REST adapter รอบหน้าโดยไม่ต้องแก้ business logic.
- 2026-05-23: Architecture polish + docs sync รอบจบ session — สร้าง `src/storageAdapter.ts` (StorageAdapter interface + LocalStorageAdapter + MemoryAdapter + swappable singleton) ตาม PRD Section 6 mandate "domain service → storage adapter → localStorage/future API"; refactor `cashflow.ts` ใช้ adapter เป็น reference (load/save ลดจาก 28 บรรทัด เหลือ 11 + delegated try/catch ให้ adapter); เพิ่ม Open Graph + Twitter card meta ครบใน `index.html` (og:image=1200x630, locale th_TH + en_US alt, canonical url) สำหรับ Facebook/LINE share; sync `docs/PRD.md` App Map ให้ตรงกับ `src/apps.ts` (12+1 apps พร้อม route ปัจจุบัน hub=/hub admin=/admin), tick P0-002/P0-008/G-003 done + P0-007/A-002 in_progress.
- 2026-05-23: Workspace shell consistency รอบจบ — ยืนยัน WorkspaceTopbar (cf-topbar พร้อม "← เว็บหลัก" bridge + language toggle + version selector + breadcrumb) render ก่อน BuildDocs-specific `.topbar` แล้ว ไม่ต้องเพิ่ม code (BuildDocs ได้ bridge อัตโนมัติ); reorganize sidebar app groups จาก 4 → 6 (Project/Business/Design/Agent/Social/Platform) เพื่อให้ทุกแอป (รวม `library`, `socialFeed`, `admin`) มี slot ใน sidebar; เพิ่ม TH/EN dictionary ของ group ใหม่. แกน skeleton ของแพลตฟอร์มครบสมบูรณ์.
- 2026-05-23: Access gate + Admin Console + funnel ครบ — เพิ่มแอป `admin` ใน manifest (routeBase `/admin`, taxonomy `platform/admin_only`, status `prototype`), subnav 4 tabs (overview/plans/overrides/audit); สร้าง `src/AdminPanel.tsx` (1147 บรรทัด, sub-agent A) ครบ tab พร้อม inline form `upsertPlan`/`upsertAppAccessOverride` + audit feed; เพิ่ม `WorkspaceAccessGate` ใน `WorkspaceAppPanel` — เรียก `evaluateAppAccess()` ก่อน render และ block preview/none/deny ด้วยหน้า upgrade ที่มี 3 paths (เปลี่ยนแผน → /support-plans, ขอ override → /admin, browse free apps → /apps); seed subscription default เป็น Support Monthly สำหรับ v0.1 dev (สลับเป็น Free ที่ /support-plans เพื่อทดสอบ gate). Tick M-005 done. Funnel `landing → /apps → workspace → upgrade prompt → /support-plans → /account` ครบทุก step.
- 2026-05-23: Membership scaffold + Workspace bridge + Logo cleanup — สร้าง `src/membership.ts` (1059 บรรทัด: Plan/Subscription/AppAccessRule/AppAccessOverride/AuditLog types + seedPlans + storage + `evaluateAppAccess()` ตาม PRD Section 5 evaluation order); เพิ่ม `/account` + `/support-plans` ใน PublicSite พร้อม access matrix table (apps × plans live preview ผ่าน `evaluateAppAccess`); ปุ่ม Activate เปลี่ยน subscription ทันที + เขียน audit log. เพิ่มปุ่ม "← เว็บหลัก / Public" ใน workspace topbar นำกลับไป `/`. แก้ `buildbybim-logo.svg` ใหม่เป็น monochrome B ตาม brand palette (PRD Section 10) แทนตึก isometric teal เดิมที่ไม่ match กับ landing monochrome และ bg color เพี้ยน. Tick M-001 + M-002 done.
- 2026-05-23: App taxonomy refactor — เพิ่ม taxonomy types ใน `src/apps.ts` ครบตาม APP_TAXONOMY.md Section 2-9 (category, profession_tags, monetization, access_level, privacy_level, ai_usage) พร้อม TH/EN copy dictionaries; tag ทั้ง 12 apps; AppsCatalogPage รับ multi-dim filter 3 แกน (สถานะ × หมวด × แผน free_like/paid_like) พร้อม taxonomy badges (category/monetization/AI/privacy) บนการ์ดทุกใบ; foundation พร้อมต่อ MembershipAccessRule รอบถัดไป
- 2026-05-23: Public routing layer + route shells — แยก public/workspace ใน `src/main.tsx` ผ่าน `isPublicRoute(pathname)`, ย้าย workspace hub จาก `/` ไป `/hub` (อัปเดต `src/apps.ts`), สร้าง `src/PublicSite.tsx` (~1000 บรรทัด) ที่เก็บ router + shared nav/footer + 6 subpages (`/apps` ดึงจาก manifest live, `/tools`/`/prompts`/`/workflows`/`/pricing`/`/developer` เป็น stub พร้อม PRD reference ใน lede), TH/EN dictionary ครบ; landing nav (`/`) intercept `<a href="#...">` แปลงเป็น `pushState` ไปยัง real route + Sign in/CTA ใน nav-cta นำไป `/hub`. ทำให้ PUBLIC_ROUTES.md First Implementation Cut ครบครัน 6 routes (`G-017` done).
- 2026-05-23: Cashflow App v0.1 prototype — `docs/CASHFLOW_PRD.md` (sub-PRD ครบ Section 1-10 ตาม Development Contract), CashflowPanel UI ที่ `/cashflow?tab=overview&version=0.1` พร้อม quick-add form (draft + confirm), entry list, summary 6 tiles (income/expense/net/draftCount/totalEntries/lastEntry), TH/EN dictionary ครบ; เปลี่ยน status `cashflow` ใน `src/apps.ts` จาก `planned` เป็น `prototype` และอัปเดต storage key ให้ตรงกับโมดูล data layer (`cashflow.entries.v1`). Forecast/Reports tab scaffolded รอ v0.2/v0.3.
- 2026-05-23: Hub Dashboard เฟส 2 — ขยายเป็น 8 summary tiles เพิ่ม `งบที่แบ่งให้งาน` (BOQ allocated total) และ `Cashflow สุทธิเดือนนี้`; เพิ่ม action row ใหม่ 3 ตัว (`งาน BOQ ยังไม่แบ่งงบ`, `Cashflow รอ confirm`, `งวดงานพร้อมวางบิล`); สร้าง `src/cashflow.ts` (data model + storage + summarize per ERD CASHFLOW_ENTRY) และ `src/hubDashboardCopy.ts` (TH/EN dictionary + `formatHubRelativeTime`); ลบ `formatHubTimeAgo` เก่าทิ้ง ทุก hub string รวม pending actions/activity feed/relative time ผ่าน dictionary ครบ ภาษาสลับได้จริงเมื่อกดปุ่ม TH/EN.
- 2026-05-23: Hub Dashboard (`/?tab=overview&version=0.1`) เพิ่ม cross-app summary 6 tiles (docs / value / defects / employees / labor / projects), tab-aware rendering (overview/ready/pipeline), `งานที่ต้องทำต่อ` action board ที่ดึงจาก defects + draft quotes + sent invoices และ `กิจกรรมล่าสุด` feed รวม documents+defects เรียงตาม `updatedAt`. Ready tab กรองเฉพาะแอปที่มีข้อมูล Pipeline tab แยกเป็น prototype/next/planned.
- 2026-05-23: Landing preview (`/style-preview`) restored `#workflows` + `#developer` sections and added a new `#prompts` section so all six nav anchors scroll to real content. TH/EN translations cover the new prompt-library copy.
- 2026-05-23: Added Prompt Set and Content Workflow ERD entities plus `docs/APP_TAXONOMY.md` and `docs/PUBLIC_ROUTES.md` for shared app categorization and public route planning.
- 2026-05-23: Added flexible app access and admin override direction: `docs/MEMBERSHIP_ACCESS_PRD.md`, ERD access entities, data strategy, and membership tasks.
- 2026-05-22: Verified BOQ Task Linkage in browser at `/boq-data?tab=task-linkage&version=0.1`; create task, assign BOQ item, reload persistence, and delete QA task flow passed.
- 2026-05-23: Added architect-led design workflow direction: Design Brief, Moodboard, Design Options, Presentation Builder, and design-to-BIM/BOQ/Docs handoff.
- 2026-05-23: Added `docs/DESIGN_PLAN_REVIEW_PRD.md` from the `arch-brain-web` concept as a Plan Review / Architect Brain design wedge app.
- 2026-05-23: Connected Plan Review / Architect Brain to ERD, data strategy, export entity, and wedge-app roadmap.
- 2026-05-23: Added `Tools Apps` direction for small practical tools such as life planning, calculators, checklists, generators, trackers, and focused assistants.
- 2026-05-23: Added `Auto Workflow Apps` direction and `docs/FACEBOOK_CONTENT_WORKFLOW_PRD.md` for draft-first Facebook content creation.
- 2026-05-23: Added `docs/PROMPT_SET_LIBRARY_PRD.md` for shareable prompt/prompt set assets, prompt packs, variables, and access tiers.
- 2026-05-22: Added `docs/DATA_STRATEGY.md` to treat saved data as the core platform asset with ownership, structured persistence, backup/export, privacy/security, quality, and audit rules.
- 2026-05-22: Added instant utility/future memory principle: each app must solve an immediate problem quickly and turn saved data into reusable workspace assets.
- 2026-05-22: Added `docs/PLATFORM_ERD.md` and `docs/DEVELOPMENT_PLAN.md` for the future backend model and phased website/platform roadmap.
- 2026-05-22: Added Agent API/internal CLI direction for web, LINE, and admin automation; production agent actions must use backend permissions, tool allowlists, confirmations, and audit logs.
- 2026-05-22: Added wedge-app go-to-market strategy: sell practical single apps first for Thai market education, while keeping BIM/IoT as future platform architecture.
- 2026-05-22: Added cross-profession collaboration direction: construction acts as a bridge to owners, architects, engineers, vendors, admin/accounting, sales, marketing, and service teams.
- 2026-05-22: Added multi-profession expansion direction: construction starts as the first vertical, but app marketplace, profession packs, and access rules must support other careers.
- 2026-05-22: Re-scoped product direction toward Build By BIM Platform: tools, content, membership, support, and service upsell; portfolio is only a developer/about page.
- 2026-05-22: Implemented BOQ Data Task Linkage UI, local-first `boq-data.task-linkage.v1` storage, budget warning, and summary.
- 2026-05-22: Rewrote master PRD around PRD-first app-by-app development and specified BOQ Data Task-to-BOQ linkage.
- 2026-05-22: Added `agentChat` Work Hub card and local Agent Chat prototype for webchat, file intake, and API channel planning.
- 2026-05-22: Corrected scope: personal debt payoff is owner goal, not product scope; parked `debtPlanner`.
- 2026-05-22: Added central docs for multi-agent development.
